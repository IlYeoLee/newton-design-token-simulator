# 인수인계 — 2026-08-02 · 투명배경 4K 통추출 파이프라인

**목표(유저)**: 뉴턴 시뮬레이터의 **인물 + 투사 UI + 판정토큰**을 피그마 프레임처럼 **정면 뷰**로,
배경·그림자·부수 요소 없이 **투명 배경 4K 영상 한 개**로 뽑는다. 에펙에서 바로 투명으로 열려야 한다.

## 결과물

`out/final3s/` — 종목별 **ProRes 4444 .mov 한 개씩** (에펙에 그대로 임포트).
같은 폴더의 `_preview.mp4` 는 눈으로 빨리 보는 용도(알파 없음), `_png` 는 원본 시퀀스.

| 종목 | 스테이지 | 해상도 | 길이 | 용량 | 불투명 |
|---|---|---|---|---|---|
| 러닝 | A3 · 하이니 | 2302×3841 | 3초 60fps | 469MB | 9.6% |
| 농구 | BK_B3 · 스텝백 2/4 | 2302×3841 | 3초 60fps | 216MB | 3.8% |
| 복싱 | BX_B3 · 잽 스윕 | 3840×2363 | 3초 60fps | 2.9GB | 49.8% |

복싱의 불투명 49.8% 는 벽 UI 의 원근 그리드·글로우가 화면 절반을 덮기 때문이다(무대가 아니라
투사 UI 자체). 네 모서리는 셋 다 알파 0 이다.

지면은 대지 1600×2670 비율이라 **세로 4K**(긴 변 3841), 벽은 2600×1600 이라 **가로 4K**다.
16:9 로 맞추면 늘어난다 — 대지 비율을 지키는 게 맞다.

**검수는 파일에서 직접 한다**(에펙에서 열어 보고 아는 건 너무 늦다):

```bash
node scripts/verify_alpha.mjs out/final3s/*.mov
# 모서리 알파 0/255 (투명 ✓) · 불투명 픽셀 3~10%
```

.mov 를 다시 디코드해서 네 모서리 알파와 불투명 픽셀 비율을 잰다.
모서리가 0 이 아니면 배경이 불투명한 것이고, 불투명이 0% 면 빈 영상, 100% 면 검은 배경이다.

## 다시 뽑는 법

```bash
npx vite --port 5199 --strictPort --host 127.0.0.1     # 먼저 켤 것
powershell -File scripts/run_final3s.ps1               # 3종 전부
```

낱개로:

```bash
node scripts/export_video.mjs --sport basketball --session --stage BK_B3 --play \
  --flat --alpha --alphafloor 0.16 --t0 1 --dur 3 --fps 60 --w 2302 --out out/final3s
```

## 이번에 고친 것 — 다시 밟지 않게

### ① 인물이 안 나오던 진짜 이유 — 헤드리스가 `paused` 를 못 푼다

내보내기는 `<video>` 를 정지시키고 `currentTime` 을 프레임마다 직접 찍는다(안 그러면 인물만
12~30배로 빨라진다). 그런데 **앱은 '재생 중인가'로 인물을 그릴지 정한다**:

```js
uLive = (readyState >= 2 && !ended && !paused)   // 복싱 벽 인물
uReady                                            // 코치 클립 판도 같은 역할
```

`paused` 가 영원히 true 라 게이트가 닫힌 채 남았다 — 복싱 벽 인물이 아예 안 그려지고
**흐린 열구름(uHeat 필드)만** 남았다. 07-31 최종본의 "주황색 구름"의 정체가 이것이다.

**고침** — `paused` getter 를 false 로 덮는다(`evaluateOnNewDocument`).
유니폼을 밖에서 덮어쓰는 방법은 **안 된다** — 앱이 매 틱 자기 값으로 다시 쓴다.

### ② 러닝 인물이 사라지던 것 — `_followLatch`

①을 고치자 이번엔 러닝 A2·A3 에서 인물이 사라졌다. 앱은 '관찰이 끝났다'고 판단하면
(`session._followLatch`) 코치 판을 끈다. 실사용에선 맞는 동작이지만 내보내기의 목적이 그 인물이다.
→ 프레임마다 `session._followLatch = false`.

### ③ 배경이 새던 것 — 휴리스틱 유지 필터를 화이트리스트로

예전 규칙은 "셰이더거나 선이면 투사광" + 이름 블랙리스트(`courtLines·courtZones·hoop`)였다.
무대에 새 요소가 생기면 **반드시 샌다**. 실제로 두 번 샜다:

- 농구 코트 사이드라인 — `LineSegments` 라 '선 = 투사광' 규칙 통과
- 복싱 원근 그리드 — `uGrid/uLines/uScan` 셰이더라 '셰이더 = 투사광' 규칙 통과

**고침** — 아는 서명만 남기고 나머지는 전부 끈다. 판별은 **유니폼 키**로 한다:

```
KEEP  uTrail uCropOff uField uHT uHalo uProg uPhase uMark   (인물 · 판정토큰)
STAGE uGrid uLines uScan uBoost uAccent uHalf uKey uTint     (무대)
UI    floorGL.mesh / wallGL.mesh
```

**fragmentShader 문자열 매칭은 못 쓴다** — 이 프로젝트 셰이더는 fx-core 공용 GLSL
(`markState`·`personLook`·`refEdge`)을 통째로 붙여 쓰므로 무대 셰이더 소스에도 `uProg`·`uPhase`
같은 이름이 섞여 들어온다. `uniforms` 키는 그 재질이 실제로 가진 것만 나온다.

### ④ 프레임 전체가 알파 12/255 로 덮이던 것 — `--alphafloor`

대지 UI 패널의 검정 배경(휘도 ≈0.10)이 알파로 살아남아 **프레임 전체가 옅게 불투명**했다
(실측: 농구 불투명 97.5%, 모서리 알파 12). 마젠타 위에 올려 놓으면 눈으로는 안 보이는데
에펙에선 어두운 사각형으로 나온다. `--alphafloor 0.16`(지면) / `0.08`(벽) 로 잘라 낸다.
→ 불투명 97.5% → 3.8%, 모서리 0.

### ⑤ 빈 프레임 판정을 파일 크기에서 알파로

프리플라이트가 PNG 파일 크기로 갈랐는데(수십 KB = 빈 프레임), `--alphafloor` 를 켜니
옅은 배경 워시가 사라져 **내용이 있는데도** 6KB 로 압축돼 멀쩡한 러닝 A3 를 중단시켰다.
→ `pngjs`(이미 devDependency)로 불투명 픽셀 비율을 직접 센다. 내보내기 끝에 자동 검수도 붙었다.

### ⑥ 4K + 수퍼샘플링은 GPU 를 넘긴다

`--w 3840 --ss 2` 는 렌더면이 7680×4726 = 3600만 픽셀이다. 에러도, 컨텍스트 손실도,
삼각형 0 도 아니고 **전부 투명한 프레임**이 나온다(실측: 프리플라이트 불투명 0.00% → ss1 에서 49.5%).
인물 클립이 살아나며 RT 가 늘어 예전보다 여유가 더 줄었다.
→ `--flat` 의 SS 기본값을 `W >= 3000 이면 1` 로 바꿨다.

### ⑦ 렌더 도중 리포에 파일을 쓰면 안 된다

내보내는 동안 리포 루트에 이 문서를 쓰다가 vite 가 소스 변경으로 보고 **페이지를 새로고침**했고,
`window.__dbg` 가 사라져 `✗ --flat 실패: 렌더 카메라가 undefined` 로 죽었다.
`vite.config.js` 의 `watch.ignored` 가 `out/` 은 막지만 루트의 새 `.md` 는 안 막는다.
**렌더가 도는 동안 리포에는 아무것도 쓰지 말 것.**

### ⑧ 스테이지가 태그에 없어 서로 덮어쓰던 것

같은 종목의 다른 스테이지가 같은 파일명으로 나왔다(실측: 7개를 훑었더니 종목당 마지막 것만 남음).
→ 태그에 스테이지 포함.

## 스테이지 고른 근거

'인물 + UI + 판정토큰'이 **동시에** 나오는 화면은 생각보다 적다.

- 실전(C2) 계열 — 판정토큰은 많은데 **인물이 없다**(라이브 = 코치 클립 꺼짐, 러닝은 3D 봇이라 무대).
- 워밍업(A1~A3) — 인물·UI 는 좋은데 토큰이 늦게 나온다.
- **BK_B2~B5(스텝백 학습)** — 셋이 전부 나온다. 앱이 이 4페이즈에서만 코치 실루엣을
  '따라하기' 화면에도 남기기 때문(피그마 143:444). 그래서 농구는 BK_B3.
- 러닝 A3 는 `--t0 0.6` 으로 시작 — t<1 이 통째로 빈 프레임이다(실측 불투명 0.00%).
  3초 안에서 시범 인물 → 발자국 토큰으로 넘어간다.
- 복싱 BX_B3 은 ①을 고친 뒤 벽 인물이 살아났다.

## 에펙에서

ProRes 4444 는 **스트레이트(비프리멀티) 알파**다. 검정·흰색 양쪽에 올려 확인했고 가장자리에
검은 테두리나 헤일로가 없다. 혹시 테두리가 보이면 `Interpret Footage → Alpha → Straight (Unmatted)`.

투사는 가산광이라 알파 = 빛의 세기다. **Screen/Add 로 얹으면 실사 합성이 자연스럽고**,
Normal 로 얹어도 배경은 완전히 비어 있다.

## 남은 것

- 복싱 4K 는 `--ss 1` 이라 얇은 그리드 선에 계단이 조금 있다. 더 깨끗하게 하려면
  `--w 2600 --ss 2`(대지 원본 배율) 로 뽑아 에펙에서 올리거나, 타일 렌더가 필요하다.
- 러닝은 3초 안에 '인물'과 '토큰'이 시간축으로 갈린다. 한 프레임에 둘 다 담고 싶으면
  러닝은 P 계열 대신 A3 후반 + 인물 유지 옵션이 따로 필요하다(앱 로직 변경).
- `--alphafloor` 값은 종목별 상수로 박혀 있다(지면 0.16 · 벽 0.08). 대지 패널 배경 휘도가
  바뀌면 다시 재야 한다 — `verify_alpha.mjs` 의 모서리 알파가 그 지표다.
