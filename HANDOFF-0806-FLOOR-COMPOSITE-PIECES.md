# 인수인계 — 2026-08-06 · 지면 UI 를 실사에 얹기 (조각으로 뽑기)

**목표(유저)**: 러닝 A2(종아리 스트레칭)를 실사 영상 위에 얹는다. 완벽한 자동 합성이 아니라
**"내가 디자인한 걸 에펙에 정확하게 입히기"** 가 목적이다. 움직임은 에펙에서 직접 준다.

---

## 1. 결론 — 판 하나가 아니라 **조각으로** 나눠 뽑는다

한 장으로 뽑아 얹으면 바닥에 **빨간 그림자 박스**가 생긴다. 원인은 합성 수식이다.

```
알파 오버 :  결과 = 색 × 알파 + 배경 × (1 − 알파)
                              ↑ 이 −알파×배경 항이 판 전체를 깎는다
가산      :  결과 = 배경 + 색 × 알파        깎는 항이 없다
```

투사 UI 안에는 **성질이 다른 두 종류**가 섞여 있다.

| | 정체 | 커버리지 알파 | 에펙 |
|---|---|---|---|
| 인물 · 발자국 · 원형토큰 · 타이틀 | 셰이더가 그리는 **실체** | 있음 | **Normal** (잉크) |
| 원형 팡 · 발모양 파형 | `AdditiveBlending` 로 그리는 **빛** | **없음** | **가산(Add)** |

빛은 커버리지 알파가 원리적으로 없다. 휘도로 알파를 지어내면 어두운 자락이 물감이 되어
바닥을 칠한다. 그래서 **판에서 빼고 따로 얹는다.**

시도했다가 실패한 것 (다시 하지 말 것):

```
알파 재인코딩(빛 인코딩)   자락 색은 맑아졌지만 −알파×배경 항이 남아 박스 그대로
블룸 끄기 (--bloom 0)      껐는데 그대로. 후처리가 아니라 셰이더가 그리는 실체였다
휘도로 잉크/빛 분리         파문 필드 0.29~0.51 > 인물 0.19. 자르면 인물이 먼저 날아간다
AE 블렌딩 모드만 바꾸기     Add 는 밝은 바닥에서 날아가고, Screen 은 파문이 사라진다
```

---

## 2. 산출물 — `바탕화면\뉴턴_팡_AE`

```
① 판 (AE Normal).mov            153MB · 1600×2670 · 270프레임 · 알파
② 팡 (AE 가산).mov                7MB · 512×512  · 31프레임
③ 발파형 — 위/아래 발자국 (가산)    4MB · 512×721  · 30프레임
④ 발자국 A/B (AE Normal)          34/29MB · 560×560 · 중앙 고정
   각각 PNG 시퀀스 동봉 · 전부 bt709 태그
배치좌표.txt
```

**배치 (1600×2670 판 좌표계, 컴프가 다르면 비례 환산)**

```
원형 팡        중심 (764, 1024)   지름 1425px      ← sizeM 0.52m × 2 ÷ 0.73mm/px
발파형 위      중심 (582,  982)   폭 648px
발파형 아래    중심 (1064, 1485)  폭 648px
터지는 시각    판 시퀀스 f153 (t0=9s 기준 14.11초)
```

두 발자국은 **같은 모양의 좌우 반전**이다(실측: 그대로 겹침 58.6% · 반전 겹침 97.2%).

---

## 3. 새로 만든 내보내기 플래그 — `scripts/export_video.mjs`

```bash
--layer noburst   원형 팡만 빼고 전부      # 잉크 판
--layer burst     원형 팡만                # 빛
--norip           발모양 파형(uRip) 끄기
--pin             발자국을 봇 발 추적에서 떼어 설계 좌표에 고정
```

`--layer burst` 판별은 `uIntensity|uEmber|uForward` 로 한다 (effects.js 전용).
**`uFPOrigin` 류를 쓰면 안 된다** — 1인칭 빔 클리핑용이라 토큰 재질도 다 갖고 있어서,
그걸로 잡았다가 판정토큰이 통째로 팡 레이어로 넘어갔다.

최종 판을 뽑은 명령:

```bash
node scripts/export_video.mjs --scene A2 --flat --alpha --alphagamma 0.5 \
  --alphafloor 0.05 --play --pin --norip --layer noburst \
  --t0 9 --dur 9 --fps 29.97 --w 800 --out out/A2_FINAL
```

### `--alphafloor 0.05` 가 중요하다

`--alphagamma 0.5` 는 어두운 톤을 살리려고 알파를 들어올리는데, **거의-검정인 배경까지**
같이 올려서 판 전체에 알파 20~30 짜리 베일이 남는다. 이게 배경을 미세하게 깎고, 노이즈라
압축이 안 돼 용량을 몇 배로 부풀린다.

```
베일 있을 때   불투명 48%   PNG 1.2GB   ProRes 3.1GB
바닥컷 후      불투명 3.4%  PNG  97MB   ProRes 153MB
```

**ProRes 4444 가 무손실 PNG 보다 커지면 뭔가 잘못된 것이다.** 그때는 알파를 의심할 것.

---

## 4. 새 스크립트

| 스크립트 | 하는 일 |
|---|---|
| `bake_burst.mjs` | 원형 팡을 **계산**으로 굽는다. `effects.js BURST_FRAG` 와 같은 수식 |
| `bake_footrip.mjs` | 발모양 파형. 실루엣을 **뽑아둔 판에서 따서** 거리장을 만든다 |
| `cut_footmarks.mjs` | 판에서 발자국만 중앙 고정으로 잘라낸다 (재렌더 없음) |
| `relight_alpha.mjs` | 알파를 빛으로 재인코딩 (**실패한 접근** — 기록용으로 남김) |
| `alpha_floor.mjs` | 옅은 베일 끊기 (익스포터 `--alphafloor` 로 대체됨) |
| `split_ink_light.mjs` | 휘도로 잉크/빛 분리 (**실패한 접근**) |
| `probe_a2_burst.mjs` | A2 팡이 터지는 시각 측정 |

**팡·파형은 렌더가 아니라 계산이다.** 세기·크기·색을 바꿔도 20분이 아니라 3초다.

```bash
node scripts/bake_burst.mjs --intensity 1.2 --halo 1.4 --ember 0.6 --noise 0 --dur 1.4
node scripts/bake_footrip.mjs <판PNG> --gain 1.5 --dur 1.4
```

---

## 5. 시뮬레이터 수정

**`fx-core.js` — 진행 아크 감김 (`uArcRev`)**
지면 토큰은 쿼드가 바닥에 누워(−90° X) 감김이 반대로 읽혀 *"먼 쪽에서 반시계로 크게 그리며"*
등장했다(유저). 유니폼으로 빼고 **농구만** 뒤집는다 (`main.js switchPack` 에서 `FXP.arcRev`).
매 프레임 주입한다(`tokens.js`) — 생성 시점에만 넣으면 팩을 바꿔도 기존 토큰이 옛 값으로 남는다.

**`main.js` — 코치 클립 깜빡임**
`uReady` 판정에 *"첫 표시 후 래치"* 라는 주석만 있고 **실제 래치가 없었다.** 루프로 되감기면
`currentTime > 0.03` 이 1~2프레임 깨져 인물이 사라졌다(유저: 션 영상이 깜빡인다).
`co._shown` 래치를 실제로 넣었다. 첫 등장만 엄격하게 보고, 영상이 죽었을 때만 다시 숨긴다.

**`session.js` — `pinMarks`**
A2 발자국을 봇 발 추적에서 떼어 설계 좌표(x ∓0.16, z −1.15)에 고정한다.

---

## 6. 남은 것 · 알려진 문제

**① `--pin` 이 실제로는 안 먹었다**
플래그를 넣고 렌더했는데 발자국이 여전히 543px 움직였다(연결요소 추적 실측).
`window.__pin` → `__isolate3d` → `session.pinMarks` 경로 어딘가가 끊긴 것으로 보인다.
지금은 `cut_footmarks.mjs` 로 **사후 중앙 고정**해서 우회했다.

**② 홀드 링을 못 뺀다**
실루엣을 따라 차오르는 진행 스트로크가 `fx-core.js` 에 유니폼 없이 박혀 있다(`holdA`).
유저는 *"안 예쁘다, 빼고 새로 만들어달라"*. `uRip` 처럼 `uHoldRim` 을 하나 두면 된다.
그 다음 `bake_footrip.mjs` 와 같은 방식으로 새로 구우면 된다.

**③ 1인칭 원근이 실사와 안 맞는다**
빛 웅덩이 모양이 레인 수렴선과 어긋난다 — 합성티의 8할이 이것이다.
지금 `--tilt` 는 감으로 준 값(A 0 · B −0.35 · C −0.6)이다.
**실사 레인선의 소실점에서 카메라 피치·화각을 역산해 수치로 넣어야 한다.** 30분 작업.

**④ puppeteer 간헐 크래시**
270프레임 렌더가 두 번 중간에 죽었다(`export_video.mjs:686` page.evaluate).
짧은 렌더는 멀쩡하다. 이 리포에 이미 기록된 미해결 건과 같은 것으로 보인다.

---

## 7. 소스 영상

```
러닝 실사(시나리오)   C:\Users\user\Videos\러닝-합성전.mp4
                     3840×2160 · 29.97fps · 29.6초 · 다중 패널 몽타주
                     ★ 정확히 x=1440 에서 갈린다. 왼쪽 1440×2160 이 1인칭 자리
러닝 트랙 POV        Downloads\선전달추가00254030 / 00254335.mov  (4096×2160 25fps)
농구 코트            Downloads\뉴턴여앗ㅇ\PRESS ON _ #3-5 / #3-7.mov  (조립식 타일 코트)
```

농구 코트는 타일 격자가 규칙적이라 평면 추적이 거의 공짜다. 흰 코트 라인으로 실측 스케일도 나온다.
