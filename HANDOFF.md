# 인수인계 — 2026-07-28

새 세션에서 이 파일만 읽으면 이어서 작업할 수 있게 쓴 문서.
관련 문서: `BEAM-OCCLUSION.md`(차폐 실측), `CURRY-5CUT-PLAN.md`(스텝백 기획).

리포: `IlYeoLee/newton-design-token-simulator` · 최신 커밋 `3ee6006` · 푸시 완료 · 트리 클린
운영: **유저는 5199(vite dev)에서 본다.** 수정 후 `npx vite build`(4173/dist 동기화)하고
"5199 새로고침"이라고 알릴 것.

## 작업 방식 (유저 지시 — 반드시 지킬 것)

- **UI 배치·크기·색은 브라우저 검증 없이 코드만 넣고 넘긴다.** 스크린샷 확인, 레퍼런스
  픽셀 측정, 크기 미세조정 재확인 전부 금지. 유저가 새로고침해서 보고 한마디 하면 끝난다.
  브라우저는 "숫자로만 확인되는 것"(안 보이는 원인, 타이밍, 물리, 콘솔 에러)에만 띄운다.
- **하드코딩 금지.** 발자국·링·화살표는 전부 시뮬레이터 디자인 시스템 토큰
  (`FootMark` / `floorRing` / `floorArrow`=makeFlowArrow)을 쓴다. CSS로 모양을 그리지 말 것.
- **크기는 기존 지면 UI에 쓰인 값 그대로**(FootMark 0.62). 피그마 픽셀 규격으로 환산하지 말 것.
- 매 단계 한 줄씩 실시간 보고.

## 지금 상태 — 스텝백 4페이즈 따라하기

관찰 프리뷰 5초 → 따라하기. 따라하기 화면(피그마 143:444) 구성:

| 요소 | 구현 |
|---|---|
| 타이틀 | 영문(`public/ready-view/floor-scenes.js`) |
| 코치 영상 | 3D 패널(그린스크린 크로마키 + 룩 LUT), 따라하기에선 scale 0.46 |
| 발자국 | `FootMark` L·R 페어, 0.62, 1.6s 사이클 들썩(R 0.18s 지연) |
| 원형 판정 링 | `floorRing` mL·mR을 발자국 아래로, 들썩임에 밝기 동기 |
| 화살표 | `floorArrow` — 그 단계에서 움직이는 발 방향 (2/4 →, 3/4 ←, 4/4 ↑) |

구현 위치: `src/session.js`
- `_beamLocal(u, v, ref)` — 빔 창 정규좌표 → 참조 오브젝트와 같은 그룹의 로컬 좌표.
  **그룹마다 부모 오프셋이 달라서 로컬 z를 손으로 계산하면 창 밖으로 나간다.**
  (이게 마크가 안 보이던 진짜 원인: 창 밖 → 빔 페더가 `uFade`를 0으로 깎음. B2 0.00 / B4 0.22)
- `BK_B2` 브랜치 = 1/4, `BK_B3/B4/B5` 공용 브랜치의 `if (POSE)` 블록 = 2~4/4.
- 마크 줄은 `V = 0.34`(창 하단, 영상 아래). 더 앞으로 빼면 카메라 코앞이라 거대해진다.
- `POSE` 맵(피그마 141:204/230/252/274 정규 좌표)에서 페어 중심을 빼고 `K=2.2`로 펴서 u로 쓴다.

`src/main.js`
- `PHW` = 영상 구간, **누적식**: B2 0~0.60 / B3 0~1.47 / B4 0~1.81 / B5 0~3.10.
  구간만 반복하면 앞 동작과 안 이어져 따라하기 어렵다(유저).
- 마지막 프레임에서 1초 정지 후 루프(`co._holdUntil`). 재생속도 프리뷰 0.25 / 따라하기 0.45.

## 남은 일

| # | 내용 |
|---|---|
| 24 | **투사면이 통째로 주황 그라디언트로 칠해지는 회귀** — 코치 영상 텍스처가 비면 크로마키를 통과해 판 전체가 LUT 색이 된다. 농구뿐 아니라 복싱 지면 UI에서도 발생(유저 확인). 시킹/정지 중 텍스처 공백이 원인으로 추정. 미수정 |
| 25 | 실전(BK_C2)을 4페이즈 시각언어로 통일 — 판정은 유지, 표현만 |

## 빔 차폐 (결론만)

현행 하드웨어(종아리 상부 밴드·바깥면 포드)로 충분. 평균 1.4% / 최악 16%.
드리블 한 번에 45ms 깜빡 수준. 발광부 추가·마운트 높이 변경 없이 간다.
근본 해법 논문: Shadowless Projector (IEEE VR 2019) — 개구 폭 26cm면 umbra 소멸.
상세는 `BEAM-OCCLUSION.md`.
