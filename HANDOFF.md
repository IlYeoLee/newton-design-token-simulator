# 인수인계 — 2026-07-27

새 세션에서 이 파일 하나만 읽으면 이어서 작업할 수 있게 쓴 문서.
관련 문서: `BEAM-OCCLUSION.md`(차폐 실측 원본), `KNOWN-ISSUES.md`(미해결 버그).

리포: `IlYeoLee/newton-design-token-simulator` · 최신 커밋 `b45e11b` · 작업 트리 클린
운영: 유저는 **4173**(preview, dist 고정 빌드)에서 확인, **5199**(vite dev)는 작업 전용.
수정 후에는 `npx vite build` 하고 "4173 새로고침"이라고 유저에게 알릴 것.

---

## 1. 화면 가림(빔 차폐) 이슈 — 현재 상황

### 무엇이 문제인가

무릎 아래 종아리 밴드에 달린 프로젝터가 바닥에 UI를 투사하는데, 드리블하는
농구공이 **조리개에서 0.13m** 앞을 지난다. 이 거리에서 지름 0.24m 구는 빔의 거의
전 각도를 덮어서, 순간적으로 투사 전체가 어두워진다.

### 실측값 (시뮬레이터 라이브, 조리개 = 종아리 바깥면 포드 위치)

| 단계 | 평균 차단 | 비고 |
|---|---|---|
| B1 로우 드리블(오른손) | **0%** | 313프레임 무차단. 광원이 왼다리라 공이 항상 반대편 |
| B2 크로스오버 | **6.1%** | 공이 중앙을 지날 때 순간 전면 차단 |

지속 시간 (11초 관측):

| 강도 | 횟수 | 평균 지속 | 최장 |
|---|---|---|---|
| 전면 암전(≥90%) | 7회 | 45ms | 68ms |
| 절반 이상 | 7회 | 73ms | 101ms |
| 조금이라도 | 24회 | 63ms | 169ms |

**해석**: 드리블 한 번에 한 번꼴로 45ms 깜빡. 화면이 꺼졌다기보다 "그림자가 스쳤다"
수준. 평균 6%는 "바닥의 한 지점이 시간의 6%만 어둡다"는 뜻.

### 시뮬레이터에 구현된 것 (`src/main.js`)

- `_apertureWorld()` — 조리개를 무릎 뼈가 아니라 **종아리 바깥 6cm · 아래 4cm**
  실제 포드 위치로 보정. 뼈 중심으로 재면 하드웨어보다 나쁘게 나온다(6% → 8%).
- `applyBallOcclusion()` — 조리개→공 기하로 차단율을 계산하고, 차단 비율만큼
  **스테이지 그룹 전체 + 지면 CSS3D 프레임을 동시에 감광**(최저 6% 잔광).
  일부 토큰만 끄는 방식은 폐기(유저 지시).
- 링은 ShaderMaterial이라 `material.opacity`만 낮추면 화면이 안 변한다.
  **`uniforms.uGain`도 같이 낮춰야 한다** — 안 깜빡이던 원인이 이것이었다.
- HUD(`#geom-info`)에 실시간 표시: `빔 차폐 지금 X%(오른손/왼손/중앙) 평균 X% 최악 X%`
- 검은 그림자 원반 그래픽은 만들었다가 폐기(유저: 투사면 밖 그래픽 금지 원칙).

### 기각된 해법 (측정으로)

- **가이드 배치 변경** — 필드를 좌우 0.6m·깊이 1.5m 옮겨도 차단률 불변.
- **좌우 중복 표시** — 대칭 두 점이 동시에 가려지는 비율 10%.
- **240Hz 추적·예측 투사** — 빛이 막힌 자리는 속도로 못 채운다. 그림자가
  중앙값 14.7m/s(상위 10% 151m/s)로 쓸고 지나가고 순간 최대 차폐 92%라
  콘텐츠를 피신시킬 자리도 시간도 없다.
- **발광부 추가** — 유저가 금지(부품 추가 불가).
- **마운트 높이 변경** — 유저가 금지(산업디자인 확정).

### 유효한 해법 (논문 근거)

**Shadowless Projector** (Hiratani·Iwai·Punpongsanon·Sato, IEEE VR 2019,
https://ieeexplore.ieee.org/document/8798245/) — 수술등 원리. 마이크로 미러 어레이
플레이트를 대구경 렌즈처럼 써서 빛을 넓은 입사각에서 내보내면 작은 물체가 모든
광선을 막을 수 없어 **umbra가 사라지고 penumbra만 남는다.** 프로젝터 1대, 연산 보정 불필요.

우리 기하 계산: umbra 완전 소멸에 필요한 개구 폭 ≈ **26cm**(공 24cm, 조리개–공 13cm,
조리개–바닥 1.5m). 밴드를 따라 곡면 스트립으로 **10~15cm만 확보해도 하드 섀도 절반 이하**.

함께 쓸 수 있는 5년 로드맵 근거:
- 메타-홀로그래픽 줌 마이크로 프로젝터 0.18cm³ (Nature Comms 2025)
  https://www.nature.com/articles/s41467-025-65764-2
- LC-메타렌즈 연속 줌, 10V 미만 (ACS Photonics)
  https://pubs.acs.org/doi/10.1021/acsphotonics.4c01530

**대외 설명 포지션**: "구멍 개수가 아니라 크기를 바꾼다. 점광원 → 면광원."

---

## 2. 커리 시그니처 재설계안 (B 훈련 2단계 + C 실전 3단계)

### 왜 바꾸나

현재 B/C가 핸들 기본기(로우 드리블·크로스오버·다리 사이)뿐이라 "커리 팩"이라기엔
약하다(유저 지적). 스텝백과 비하인드백 풀업을 넣어야 시그니처가 선다.
부수 효과로 **스텝백·비하인드백은 판정 대상이 발과 몸이라 빔 차폐가 0%**다.

### 훈련(B) — 2단계로 축소

| 단계 | 내용 | 지면 UI | 차폐 |
|---|---|---|---|
| **B1 로우 드리블** | 원형 마크 위 오른손 10회 → 시선 바깥으로(메트로놈) | 중앙 원형 존 + 카운트 링 | 0% |
| **B2 크로스오버** | 좌우 존 교대 바운스 8회 | 좌우 존 ±0.22 + 중앙 숫자 | 6% (45ms 깜빡) |

기존 B3(다리 사이)는 실전 시퀀스에 흡수하고 단계에서 제외.
두 단계 모두 앞에 관찰 프리뷰 5초(코치 실루엣 + Preview 필 + 카운트다운) 유지.

### 실전(C) — 3단계

| 단계 | 내용 | 지면 UI | 차폐 |
|---|---|---|---|
| **C1 크로스오버 → 스텝백** | 크로스오버로 흔들고 뒤로 빠지는 발 위치 판정 | 앞 스탠스 존 + **뒤 스텝백 존**(0.5~0.7m 후방) | **0%** |
| **C2 비하인드백 → 풀업** | 등 뒤로 넘기고 바로 슛 자세 | 좌우 게이트 + 릴리즈 마크 | **0%** |
| **C3 풀 시퀀스** | 로우 드리블 → 크로스오버 → 스텝백 → 릴리즈 | 순차 점등 게이트 | 6% (크로스오버 구간만) |

### 구현 메모

- 스텝백 모캡: `rk_stepback` 클립이 이미 등록돼 있다(`src/xbot.js` 클립 레지스트리).
  CMU 102 농구 트라이얼(`public/mocap/cmu/102_*.bvh`)도 받아뒀으나 리타겟 미완(작업 #22).
- 판정: 스텝백은 **발 위치**가 판정 대상 → `xbot.getProbes()`의 발 좌표로 후방 존 히트 판정.
  기존 B3 `_gateAdvance` 패턴 재사용.
- 존 배치는 `src/session.js`의 `floorRing(x, z, rIn, rOut, color, op)` + `_mk(stageId)` 그룹.
- 빔 투사 가능 심도는 z −2.5~−4.1(BDEEP=0.75 시프트 적용). 후방 스텝백 존은 이 창 안에 둘 것.

---

## 3. 남은 작업

| # | 내용 | 상태 |
|---|---|---|
| 16 | 옆구리 화살표 좌우 방향 반전 검증 | 미확인 |
| 19 | 잔여 검은 사다리꼴 쿼드(존 위) | NaN 스크럽 이후 유저 재확인 대기 |
| 22 | CMU 102 농구 트라이얼 리타겟·실전 조합 | 다운로드만 완료 |
| — | 위 C1~C3 재설계 구현 | 착수 전 |

## 4. 오늘 끝낸 것

- 훈련 B1·B3 관찰 프리뷰 확장(#21 완료)
- 훈련 관찰 영상을 `이게진짜.mp4` 핑퐁 베이크(`public/bhandle_pp.mp4`)로 전면 교체,
  루프 깜빡임 제거(첫 표시 후 래치), 크기·비율 9:16 정정
- B1 모션을 드리블 루프2(`bp_dribble`)로 교체, 왼팔 자연 축 내림
- 공 물리: 손 체류 0.22, 낙하 30%:상승 70% 비대칭 이징, 이즈인 쿼틱
- B1 공이 바닥에 붙던 버그(엔벨로프 적응 1.2/s가 손 진동보다 빨랐음) → 0.10/s
- 빔 차폐 계측·표현 전체 (위 1번)
