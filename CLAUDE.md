# CLAUDE.md — 이 리포에서 일하는 규칙

> 새 세션·새 계정·새 기계가 **이 파일만 읽고** 이어서 일할 수 있게 쓴 문서.
> 맥락(무엇을 왜 그렇게 했는가)은 **커밋 메시지**에 있다 — 이 리포의 커밋은 근거를 본문에 적는다.
> `git log --oneline -50` 을 먼저 훑을 것. 화면별 상세는 `HANDOFF-*.md`.

## 0. 브랜치·운영

- **작업 브랜치 = `b2-slip-clip-rebuild`** (정본). `main` 은 배포 시점 표식으로만 쓴다.
- 유저는 **5199(vite dev)** 에서 본다. 수정 후 "5199 새로고침" 이라고 알린다.
- 배포: `npx vite build` → `dist/` 를 gh-pages 워크트리(`/tmp/ghp`)에 rsync → 푸시.
  `gh-pages` 로컬 ref 가 뒤처져 있으면 `git reset --hard origin/gh-pages` 먼저.
  **rsync 에 `--delete` 를 붙이지 말 것.** gh-pages 에는 이 리포가 추적하지 않는 미디어가
  올라가 있다(`public/_bg/*.mp4` · `public/_bg/_orig/` 는 .gitignore — 수백 MB라 기계마다
  있고 없다). `--delete` 로 밀면 그 파일들이 배포에서 사라지는데, 로컬에 원본이 없는 기계는
  되돌리지도 못한다. 대가는 옛 해시 번들이 쌓이는 것뿐이고(index 가 새 해시만 참조) 무해하다.
  (2026-08-12: `--delete` 로 `bk_chroma_4k_intra.mp4`·`_orig/wall*.png` 가 삭제 대기에
  올라온 걸 푸시 직전에 발견.)
- **커밋은 자주, 세션 끝에 반드시 push.** `dev-sync.sh` 는 30초마다 `pull` 만 한다 — 올리는 건 사람이 한다.
  (2026-08-07: 다른 기계에서 2시간 작업분이 푸시 안 돼 사라질 뻔했다.)

## 1. 유저 작업 규칙

- **UI 배치·크기·색은 브라우저 검증 없이 코드만 넣고 넘긴다.** 브라우저는 숫자로만 확인되는 것
  (안 보이는 원인·타이밍·물리·콘솔 에러)에만 쓴다.
- **하드코딩 금지.** 지면 UI 는 룩시스템 토큰만 쓴다: `FootMark` · `floorRing` · `makeFlowArrow` ·
  `drawTrajectory` · `countRing`.
- **추측 금지, 계측 먼저.** 렌더링 버그는 앱 안에 계측기를 먼저 넣는다(WebGL 픽셀은 앱 안에서만 읽힌다).
  "고쳤다" 를 단정하지 말고 실측값을 함께 보고한다.
- 매 단계 한 줄씩 보고. 최소·최단 해법(ponytail) 우선.

## 2. 정본(canon)이 어디 있는가 — 값이 두 벌이면 그게 버그다

| 대상 | 정본 | 주의 |
|---|---|---|
| 마크 룩(발자국·존원) | `src/mark-look.json` ← footlab '코드에 저장' | 랩과 시뮬이 **같은 키 집합**을 봐야 한다 |
| 지면 조판·토큰 | `src/floorgl.js` `TOK` / `LAYOUT` ← tokens.html | 슬라이더 id 목록(`KEYS`)에 없는 키를 넣으면 스크립트가 죽는다 |
| 셰이더·SDF·규약 | `src/fx-core.js` (랩·시뮬 공용) | 여기 있는 코드의 원산지는 fxlab.html |
| 글리프 슬롯 | `src/fxlut.js` `DEFAULT_GLYPHS` | 사설 경로로 SVG 를 직접 물지 말 것(로고가 그랬다) |
| 스테이지·판정 | `src/session.js` `STAGES` / `CAPS`(floorgl) | `adv` 가 무엇을 세는지 정한다 |

**같은 실수의 반복 패턴**: 랩(footlab/fxlab)에 키를 추가하고 시뮬 쪽 표를 빠뜨리거나 그 반대.
`applyMarkLookTo`(tokens.js) 와 `OV_MAP`(footlab.html) 은 **항상 같이** 고친다.

## 3. 2026-08-06~07 에 확정된 규칙

- **마크 안 영문·한글·숫자 = 도트 폰트(OffBit) 고정.** 한 글자면 무조건 도트로 간다.
  도형 슬롯(`LOGO`·`FOOT_OUT_L`·`TIP_TRI`·연산기호)만 SVG. → `drawGlyph` 의 정규식이 이 규약의 몸통이다.
- **로고 글리프는 러닝 지면 마크에서만.** `GLYPHS.LOGO` 슬롯(`assets/glyphs/logo.svg`), 좌/우는 `mirror`.
- **파동(rip) = 0.** 정지 프레임에서 물결 테두리가 형태로 굳어 알파 추출을 망친다.
  세션의 파동 부스트는 **정본의 배수**여야 한다(`MARK_LOOK.rip × (1+k)`), 기준선을 박지 말 것.
- **상태 구분 = 온도 창 × 명도.** 색만으로는 안 갈린다(유채 4색뿐 + 가산 합성은 휘도가 밝기다).
  창은 `states[*].tLo/tHi`(0 쪽이 레드, LUT 0.00~0.30 이 전부 #FA3030), 밝기는 `op·w·halo·glow`.
  **아웃라인으로 상태를 구분하지 않는다.**
- **하프톤 도트는 단색 순백.** 필을 따라가게 만들면 각인이 필에 녹아 발가락·아치 형태가 사라지고
  상태마다 극성(밝은 점↔어두운 점)이 뒤집힌다.
- **컨테이너가 내용보다 먼저 자리를 만든다.** 알약 폭: 커질 때 0.03s(즉시), 줄어들 때 0.18s.
  링 슬롯도 같은 시계. 글자 크기는 **목표 폭**에서 정한다(이징 중 폭으로 정하면 글자가 흔들린다).
- **알약 상한은 둘이다**: 투사 콘 안전폭(`safeW`) **그리고 대지 폭(`W`=1600)**.
  y176 의 안전폭은 2174 로 대지보다 넓다 — 콘 검사만 통과하면 안전하다고 믿으면 잘린다.
  깊이도 상한이다: 대지는 빔보다 멀리 뻗어 있어 위쪽 289px 은 투사면 밖(`TOK.headY` 340).
- **A2(종아리) 타이머는 늘어나는 발 = 뒷발에.** `a2Cyc.workLeft` 가 정본 —
  자막(LEFT/RIGHT)·홀드 링·카운트다운·Success 가 전부 이 값을 본다. `isLeft` 는 '내딛는 발'이다.
- **A2 에는 Locked 이 없다.** 두 발이 끝까지 한 스탠스로 남아 역할만 주고받는다.
  세트 완료는 마크가 아니라 세션(알약·아크)이 말한다.
- **역할은 상태 하나로만.** 알파·크기·파동에 같은 말을 얹으면 위계를 여러 번 주는 것이다.
- **가이드 화살표는 매 턴 켠다**(실전 BK_C2 만 없음). 되감김(`_bkStrT > t`)도 재진입이다 — 래치는 그때 풀린다.
- **신호는 매 프레임 다시 정한다.** `demoActive` 처럼 true 로만 켜지는 플래그는 반드시 틱 진입에서 끈다.
  (안 끄면 관찰→따라하기 모프가 영영 안 일어난다.)
- **안 보이는 노드는 자리도 안 차지한다**(지면 조판). 링 슬롯이 빠지면 상자도 그만큼 닫힌다.

## 4. 검사기·추출 파이프라인

```
npm run check:capsule / check:floor / check:content / check:motion / check:clips
node scripts/check_pill_fit.mjs      # 알약 조판 감사 — 그릇을 넘어 줄여 그린 곳(런타임 계측)
node scripts/export_ui.mjs --stage BK_B5 --alpha --w 2048 --dur 5 [--t0 7.3]
node scripts/export_live_ui.mjs --stage BK_B5 --wait-loops 1 [--pin-loops 1] [--headed]
node scripts/alpha_floor.mjs <src> --out <dst> --th <문턱>   # 문턱은 플레이트 코너를 실측해서 정한다
```

- `export_ui.mjs` 는 세션을 **안 돌린다**(결정론) → 관찰(프리뷰)처럼 세션이 만드는 화면은 재현 불가.
  그때는 `export_live_ui.mjs`. **미해결**: 관찰 화면을 핀으로 고정하면 알약 내용(링·타이틀)이 비어 찍힌다 —
  그 값은 앱이 매 프레임 세팅하고 캡처 루프가 그 경로를 건너뛴다. 원인 위치만 특정됨.
- 알파 추출은 **어두운 실내 + 야간(가산)** 플레이트에서. 문턱을 추측하지 말고 코너 픽셀을 재라.

## 5. 열려 있는 것

- Success 온도 창을 `tHi 0.46` 이하로 내리면 몸통이 통째로 꺼지는 **절벽**이 있다(원인 미확정).
- `SAFE_TBL` 은 `fpNear .3 / fpFar 2.0` 시절 실측이다. 리그가 바뀌면 어긋난다 —
  런타임 파생이 정답이지만 대지 px → 월드 매핑을 `main.js`(UI_MASK 를 세우는 쪽)가 넘겨줘야 한다.
- 2026-08-07 01:31 이후 약 2시간 작업분이 다른 기계에 푸시되지 않은 채 남아 있을 수 있다.
