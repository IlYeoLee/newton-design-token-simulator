# 다음 세션 인수인계 (러닝/농구 지면 UI)

_마지막 업데이트: 2026-07-26 · 브랜치 main · HEAD = 농구 골대 커밋_

## 지금까지 (커밋됨)
- **러닝 학습 UI Figma 이식** (`baf8d15`): 프리뷰 화면=Figma 122-270, 훈련 화면=Figma 122-308.
  타이머 실초 카운트다운(스프린트 30→0, 가속 10→0, 1초/1틱), 타이틀 Y=176 통일.
- **배치 편집**(`3ca90bf`~`e0636a8`): 편집모드 드래그로 빔프로젝터·인물 이동, 리셋 버튼, 빔이 프로젝터 따라감.
- **농구 코트**(`bb9c50d`): 회색/검정 바닥 2종.
- **농구 골대**(방금): `src/scene.js` 절차적 골대(백보드·림·네트·기둥), 농구+코트 바닥일 때만 표시.

## ⚠️ 미해결 버그 #1 — 프리뷰 UI 잘림 (최우선)
**증상**: A2/A3(프리뷰) 화면에서 `[Preview 필 | 링]` 가로 행(`#prev-row`)의 **필이 화면 왼쪽으로 잘려 안 보임**. 링(숫자)만 작게 왼쪽에 뜸.
- 파일: `public/ready-view/floor-scene.html` (A2/A3 분기, `#prev-row` flex gap 120).
- **확인된 사실**: 보드(1600px) 안에서는 정상 중앙정렬(pill 448–818, ring 938–1152, 중심 800). 즉 HTML/CSS 레이아웃은 맞음.
- **원인 추정**: 프리뷰 카메라(코치 3인칭 시점)의 CSS3D 투사에서 보드 중심이 화면 중심으로 안 매핑됨 → 폭 ~700px 행이 왼쪽으로 밀려 잘림.
- **미완**: 보드px→화면 투사 좌표 계산을 시도했으나 `floorObj` 매핑 가정이 틀려 좌표가 무의미하게 나옴(scale=1, objPos=0 나옴 → floorObj가 CSS3D 보드가 아닐 가능성). **정확한 보드→화면 캘리브레이션부터 다시 잡아야 함.**
- **해결 방향(택1)**:
  1. 행 폭을 줄이고(필/링 축소 or gap↓) 프리뷰 카메라 가시영역 중앙에 재배치.
  2. 진짜 보드→화면 매핑 찾기: 월드좌표 아는 코치를 기준으로 캘리브레이션 후 `#prev-row` top/left 보정.
  - **주의**: P 훈련 화면(`#train-row`, 동일 flex 패턴)은 1인칭이라 OK였음. 프리뷰만 3인칭이라 깨짐 → 카메라별 위치 규칙 필요.

## 미해결 #2 — 화면 Y 통일 (유저 요청, Figma 27-2843)
"중간 성공/과정 화면은 조금 아래로, 나머지는 조금 위로 싹 통일." → Figma node **27:2843**(fileKey `a2Zo9mBTQojjGKaSerQzUa`)이 전체 화면 배치 가이드. 스크린샷만 받고 반영 못 함. 프리뷰 잘림(#1) 해결과 함께 지면 UI Y 규칙 재정립할 것.

## 미해결 #3 — P3 음성
"인터벌이에요 목소리 잘못 들어갔어" → 인터벌 단계 오디오 파일 교체 필요(미착수).

## 남은 태스크
- **#28** 1인칭 몰입 — 화면 가득·실제 시야 크기(fov ~60)
- **#29** 농구 웜업 3단계 재구성
- **#30** 농구 스텝연습 + 실전 구성
- **#31** 농구 골대 맵 + 콘 장애물 코스 (골대는 완료, 콘 코스 남음)

## 디버깅 팁
- 로컬: `localhost:5199`. 지면 프레임 = 동일출처 iframe → `floorIframe.contentDocument` 직접 조작 가능.
- 스테이지 강제이동(콘솔): `const s=window.__sess; s.stageIdx=s.stages.findIndex(x=>x.id==='A3'); s.t=1; s._enter();`
- 세션 머신: `src/session.js` STAGES.running (A워밍업→P훈련→C실전). P단계 phases 배열이 타이머·강도 정의.
- 훈련 라이브값 주입: `src/main.js` trainPhase() → `#s-title/#spm-me/#spm-tgt/#tp-arc`.
