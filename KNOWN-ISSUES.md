# 미해결 이슈 (영구 기록)

---

## RESOLVED · 카드 빨강이 죽어 보임 — inset 글로우 지우개 알파 (2026-07-30 종결)

유저가 "칙칙하다 / 연하다 / 색이 죽었다"로 여러 번 신고했고, 나는 그때마다 **엉뚱한 데를**
지목했다: 밝은 실내 투사면, 벽 평면 5% 알파, 대지 투명 배경, 인물 블렌드 모드, 블룸·주간 노출.
전부 그럴듯했고 전부 무고했다(인물 블렌드는 별개의 진짜 버그였지만 이 증상의 원인은 아니다).

**증상** — `#FA3030` 으로 칠한 완료 카드가 화면에서 연분홍으로 보인다. 지면·벽 공통.
카드는 알파 1 로 칠하므로 투사면과 섞일 수 없는데도 연했다.

**근본 원인** — `floorgl.js` `insetGlow()` 의 **지우개 fill 이 알파 0.6 이었다.**
```js
g.fillStyle = color;                       // 'rgba(255,255,255,0.6)' — 글로우 색
g.beginPath(); g.roundRect(...); g.fill(); // 오프스크린을 글로우색으로 채움
g.globalCompositeOperation = 'destination-out';
g.filter = `blur(...)`;
g.beginPath(); g.roundRect(안쪽); g.fill(); // ← fillStyle 이 아직 알파 0.6
```
`destination-out` 은 dst 알파를 `(1 − src알파)` 배로 **남긴다**. 알파 0.6 지우개는 40% 를 못 지운다
→ 림만 남아야 할 글로우가 카드 **전체**에 흰색 24% 로 깔린다. 250·48·48 → 251·98·98.

**증거(브라우저에서 3D·포스트 이전 캔버스 원본 픽셀 실측)**

| 지점 | 고치기 전 | 고친 뒤 |
|---|---|---|
| 카드 중앙 | `rgb(251,98,98)` | `rgb(250,48,48)` = #FA3030 |
| 안쪽 120px | `rgb(251,98,98)` | `rgb(250,48,48)` |
| 림 6px | `rgb(252,149,149)` | `rgb(252,134,134)` |

**중앙과 안쪽 120px 이 완전히 같은 값**이라는 게 결정적이었다 — 림 글로우라면 나올 수 없는
분포다. 이 한 줄이 "캔버스에서 이미 죽었나(=드로잉 문제), 3D·포스트에서 죽나(=합성 문제)" 를
갈랐다. 그 전까지의 추측은 전부 후자를 향하고 있었다.

**고침** — 지우개 직전에 `g.fillStyle = '#000'`(알파 1). 카드·버튼 등 inset 글로우 전부에 걸린다.

**재발 방지 규칙** — 캔버스에서 `destination-out`/`destination-in` 으로 지울 때 **fill/stroke 스타일은
반드시 알파 1** 이어야 한다. 지우개는 '모양'만 쓰고 색은 안 쓴다. 색이 남아 있으면 부분 지우개가
되고, 그 결과는 '조금 흐려짐'이라 눈으로는 버그로 안 보이고 디자인 실수처럼 보인다 — 그래서 오래 갔다.

**계측 방법(재사용)** — 이 앱의 투사 UI 는 2D 캔버스라 원격에서도 픽셀을 읽을 수 있다.
WebGL 백버퍼(`preserveDrawingBuffer:false`)와 달리 `getImageData` 가 그대로 동작한다:
```js
const M = await import('/src/wallgl.js?v=' + Date.now());   // Vite dev = ESM, 캐시 무효화
const w = new M.WallGL(); w.stage='BX_T1'; w.kind='transition'; w.t=6; w.params={};
w._paint();
w.canvas.getContext('2d').getImageData(x, y, 1, 1).data;    // 대지좌표 × K
```
앱 인스턴스가 아니라 **새 인스턴스를 만들어 원하는 화면을 직접 그리게** 하는 게 요점 —
세션 상태를 건드리지 않고, 원하는 t 로 정지시켜 잰다.

---

## RESOLVED · 투사 마크가 통째로 검은 판이 됨 (2026-07-29 종결)

유저가 **다섯 번** 신고하고 내가 **다섯 번 다른 곳을 고쳤던** 버그. 재발 방지를 위해 전 과정을 남긴다.

**증상** — 농구 드리블(BK_B1) 중 바닥에 순수 검정 사각형이 뜬다. 간헐적. 크기가 매번 다름(256×32 ~ 328×48).

**근본 원인** — `fx-core.js` `markState()` Success 분기의 `pow()` 밑이 음수가 되어 **NaN**.
```glsl
e     = 1.0 - pow(1.0 - prog, 2.6);          // prog>1 → 밑<0
fillA = pow(1.0 - (prog - 0.4) / 0.6, 1.4);  // prog>1 → 밑<0
```
GLSL `pow(x,y)` 는 x<0 에서 정의되지 않는다(대개 NaN). 구동자가 `uProg` 를 1 을 아주 살짝
넘겨 주면(실측 1.0000002) NaN 이 색·알파를 타고 흘러 판 전체가 `rgba(0,0,0,255)` 가 된다.
CPU 재현: `prog=1.0` 정상 · `prog=1.0000002` NaN.

**왜 주간에만, 왜 가끔** — 야간은 가산 블렌딩이라 NaN→0 이 더해져 아무 일도 없다.
주간 잉크(`uDay=1`)는 NormalBlending 이라 알파가 채워져 그 자리를 통째로 지운다.
그리고 `uPhase=2`(Success) 판정이 나는 순간에만 그 분기를 탄다.

**고침 세 겹** (`src/fx-core.js`)
1. `markState()` 진입에서 `prog = clamp(prog, 0.0, 1.0)` — 근본
2. `pow` 밑이 될 수 있는 `sd` 를 전부 `abs()`, `uW` 분모에 `max(·, 1e-4)` — 같은 부류 5곳
3. `markState()` 반환에 NaN 스크럽 `A *= step(vec4(-1.0), A) * step(A, vec4(1e6));`
   NaN 과의 비교는 항상 false 라 `step()` 이 0 을 고른다(GLSL ES 1.0 에서 신뢰 가능한 유일한 방법).
   투사 UI 는 가산광이라 '없음'이 안전한 기본값이다.

### 규칙 — 셰이더에서 다시는 이러지 않기
- **`pow()` 의 밑에는 반드시 `max(base, 0.0)` 또는 `abs()`.** 밑이 파라미터에서 유래하면 예외 없이.
- **외부에서 들어오는 진행값(`prog`·`t`·정규화 좌표)은 셰이더 진입 즉시 `clamp`.** 구동자를 믿지 않는다.
- **분모에 `max(·, 1e-4)`.** 룩 슬라이더(`uW` 등)는 0 까지 내려간다.
- **투사 UI 셰이더의 출력은 NaN 을 밖으로 내보내지 않는다.** 마지막에 스크럽 한 줄.

### 규칙 — 이런 버그를 다섯 턴 대신 한 턴에 잡기
`?blackprobe=1` (`src/main.js` `blackProbe()`) 를 켠다. 앱의 렌더 루프 안에서
합성 끝난 프레임을 읽어 검은 덩어리를 찾고, **씬 개체를 하나씩 꺼서 검정이 사라지는지**로
범인을 특정한 뒤 재질·유니폼·픽셀값을 화면 배지에 띄운다.

- **헤드리스로는 측정 불가** — `preserveDrawingBuffer:false` 라 `readPixels`/`drawImage` 가 전부 검정.
- **브라우저 원격 조종도 불가** — 탭이 뒤로 가면 `visibilityState=hidden` → rAF 정지.
- **레이캐스트는 증거가 아니다.** "그 방향에 뭐가 있나"는 상관, "끄면 사라지나"가 인과.
  실제로 레이캐스트는 두 번 헛짚었다(Line 판정 반경 기본값 1m 함정 → 검정을 만들 수 없는 가산 재질).

### 이미 시도해 실패한 것 (다시 파지 말 것)
- 프리멀티 알파 문턱값 게이트 `alpha *= smoothstep(0.0, 0.02, lum)` — lum=0.05 가 통과
- 프리멀티 알파 비례 불변식 `alpha = min(alpha, lum*1.6)` — 그 자체로는 옳지만 이 버그와 무관
- 비디오 프레임 게이트(`frameHasImage`·`uLive`) — 무관
- 검정 코트 표면 색(`court_black` 0x0f1420) — 무관. 다만 무광 검정이 화면에 rgb(2,2,2)로
  찍히는 건 사실이라 광택을 준 수정은 유지(`scene.js`).

---

## RESOLVED · 검은 사각 깜빡임 (2026-07-27 종결)

**근본 원인** — 2번째 WebGL 캔버스(오클루전 오버레이, zIndex 7)의 컴포지팅.
display 토글 수정으로도 재발했고, **캔버스를 DOM에서 완전히 제거한 빌드(525917f)에서
유저 실환경 깜빡임 소멸 확인**(마젠타 배경 판별 실험으로 교차검증 — 깜빡임 자체가 사라짐).

과정에서 함께 제거된 부차 원인들(모두 유효한 수정):
- 지면 프레임 iframe → same-document Shadow DOM 주입(재래스터 플래시 경로 소멸, 에이전트)
- 프레임 내부 무한 CSS 애니메이션 6종 유한화
- 세션의 프레임 DOM 매 바운스 쓰기 차단
- 전환 시 src 교체 검은 공백 → (iframe 시절) 더블 버퍼

**남은 트레이드오프** — 오버레이 제거로 지면 프레임이 봇 몸 위에 그려짐(관통).
후속 설계: 프레임이 이제 same-document div이므로 봇 발밑 영역을 clip-path(evenodd 홀)로
잘라내는 방식이 가능(2번째 GL 컨텍스트 불필요). ?occl=1로 구 오버레이 복원 가능.


> 나중에 다시 잡을 것. 재현 조건·이미 확인한 사실·이미 시도해 실패한 것을 남겨,
> 다음에 같은 길을 다시 걷지 않게 한다. 해결되면 이 파일에서 지운다.

---

## ISSUE-1 · 세션 스테이지가 뒤로 되돌아감

**증상** — 워밍업 진행 중 단계가 역방향으로 튐. 관측 로그(2026-07-27, 농구 세션):

```
4:BK_T1 @0.1
3:BK_A3 @0.1     ← 역방향
2:BK_A2 @0.1     ← 역방향
3:BK_A3 @0.3
2:BK_A2 @0.4
```

`@0.1`은 스테이지 로컬 시각 = 매번 새로 진입한 것(단순 표시 오류 아님).

**확인된 사실**

- `stageIdx`를 쓰는 곳은 `session.js` 4곳뿐: `start()`(→0), `next()`(+1), `prev()`(−1),
  `_gateAdvance()`/`reportVerdict()`(→`_firstBIdx()`). 다른 파일엔 대입 없음.
- `_firstBIdx()`는 `/B1$/` 매칭이라 농구에선 5(BK_B1). 관측된 2·3과 무관.
- `prev()` 호출부는 `btn-stage-prev` 클릭 하나뿐.
- `next`/`prev`/`_enter`/`_gateAdvance`/`start`에 스택 추적을 걸고 재실행하면
  **전부 정방향**(4→5→6→7)만 잡히고 역방향은 재현되지 않음.

**가설(미검증)** — 계측을 붙이면 사라지는 타이밍성. 세션 객체가 교체되는 경로
(팩 전환·세션 재시작)와 겹칠 때만 발생할 가능성.

**다음 단계** — 프로토타입 패치 대신 `stageIdx`에 setter를 걸어 역방향 대입만
스택과 함께 기록하는 상시 가드를 심고, 정상 플레이에서 재현 대기.
(주의: 실행 중 객체에 `Object.defineProperty`를 걸면 CDP 평가가 죽었음 — 코드에 직접 심을 것)

---

## ISSUE-2 · 좌우 궤적 토큰이 아직 동시에 움직임

**증상** — 하이니/니 드라이브에서 좌·우 궤적 토큰이 각 발 타이밍과 무관하게 함께 움직임.

**이미 고친 것 (커밋 `32ba942`)**

- 근본 하나는 잡았음: `tjTrigger(H, side, p, prev)`를 호출하기 **바로 윗줄**에서
  `H._prevPL = H._pL`로 갱신해 `p > TH && prev <= TH` 상향 교차가 **영원히 거짓**이었다.
  → 호출부 인자에 의존하지 않는 자체 래치(`_tjUpL/_tjUpR`)로 교체.
- 각 프리미티브에 `t0`(사이클 시작 시각)을 두고 `tickPrims`가 `t - t0`으로 그린다.
  그 발이 임계(0.10)를 넘는 순간 그 궤적만 `t0`을 갱신 → 사이클 재출발.

**남은 문제** — 유저 확인상 여전히 동시. 아래 중 하나로 추정:

1. `_pL/_pR`이 실제로 0.10을 못 넘는다 (프로브 스케일 문제).
2. `t0`은 갈라지는데 사이클 길이(`1/tempo ≈ 2.4s`)가 렙보다 길어 육안으로 동시처럼 보인다.
3. `p.t0` 갱신이 `tickPrims`의 30Hz 스로틀과 어긋난다.

**검증이 막힌 지점** — 자동화 브라우저에서는 봇이 무릎을 들지 않아(`_pL ≈ 0.03`)
좌우가 갈라지는 것을 눈으로 확인하지 못했다. `xbot.getProbes` 오버라이드도
해당 스테이지 틱이 돌지 않아 값이 반영되지 않았다.

**다음 단계** — `_pL/_pR`과 `t0L/t0R`을 HUD나 콘솔에 상시 노출해 실제 플레이에서 수치를 먼저 볼 것.
수치가 갈라지는데도 동시로 보이면 원인은 (2) → 궤적 `tempo`를 렙 주기에 맞춰야 한다.

---

## ISSUE-3 · 궤적 토큰이 발자국 뒤로 보임 (부분 해결, 재확인 필요)

- `renderOrder`·`depthTest`로는 해결 불가였음. **가산 블렌딩은 그리는 순서가 결과를 바꾸지 않는다**(빛의 합).
  발마크 빨강이 포화라 궤적이 묻힘.
- 궤적 패널만 `NormalBlending` + `depthTest:false` + `renderOrder 14`로 전환(`primPanel` 한 곳).
  씬에서 `blending:1(Normal)`, `depthTest:false` 확인.
- 유저 재확인 필요. 여전히 묻히면 남은 수단은 발마크 쪽 밝기를 낮추는 것.

---

## 실패 기록 · 평면(--flat) 추출의 대지 캔버스 상한 — 폭 3072 에서 전량 투명 (2026-08-06)

BK_B1 셋업(발 벌어짐 + 화살표)을 에펙 소재로 뽑으면서, **크롭하고 나서도 2K 가 되게**
원본을 크게 뽑으려고 `--w 3072` 를 줬다. 결과는 에러도 컨텍스트 손실도 아니고
프리플라이트 **최대 불투명 0.06%** — 즉 전 프레임이 사실상 빈 화면이었다.

**원인** — `--flat` 의 `UISCALE` 기본값이 폭에서 파생된다:

```js
const UISCALE = +arg('uiscale', FLAT ? Math.min(3, Math.max(1, W / FBASE[0] * 1.05)) : ...);
//   FBASE(지면) = [1600, 2670]
```

| --w  | uiscale | 대지 캔버스 | 결과 |
|------|---------|-------------|------|
| 2048 | 1.34    | 2150×3588 ≈ 31MB | ✅ 정상 |
| 2560 | 1.68    | 2688×4486 ≈ 48MB | (미검증) |
| 3072 | 2.02    | 3226×5384 ≈ 69MB | ❌ 전량 투명 |

`export_video.mjs` UISCALE 주석에 이미 적혀 있던 **66MB 함정**과 같은 벽이다. 그 주석은
4K(`--w 3840`) 기준으로 쓰여 있어서 "2K 대는 안전하다"고 읽었는데, 평면 모드는 대지 비율이
1600×2670(세로로 긴 판)이라 **같은 폭에서 4K 원근 모드보다 대지가 크다.** 폭이 아니라
**대지 픽셀 수**가 기준이다.

**대응** — 평면에서 폭을 올릴 땐 `--uiscale` 을 직접 낮춰 대지를 50MB 아래로 묶는다.
그래도 대지가 출력보다 크기만 하면 선예도는 같다(그 이상은 축소하며 버려진다).

**증상이 고약한 이유** — 실패가 조용하다. 프리플라이트가 없었으면 180프레임을 다 뽑고
나서야 알았다. 새 해상도로 처음 뽑을 땐 **짧게(--dur 1) 한 번 찔러 보고** 본 렌더를 건다.

---

## 실패 기록 · 다른 세션의 머지가 워킹트리 편집을 지웠다 (2026-08-06)

`src/session.js` 의 BK_B1 수정(카운트 10→1 · 2막 제거 · 존 z)을 워킹트리에 둔 채
추출 작업을 하는 동안, **같은 리포에서 도는 다른 세션**이
`Merge remote-tracking branch 'origin/main'` (5e723a0, 16:51) 을 돌렸고 그 파일이
원본으로 되돌아갔다. 커밋을 안 해 뒀으니 되돌릴 것도 없었다 — 손으로 다시 넣었다.

**규칙** — 이 리포는 세션이 동시에 여러 개 돈다. 소스 편집은 **끝나는 즉시 커밋한다.**
"추출 먼저 하고 나중에 같이 커밋" 이 정확히 이 사고를 만든다.
(추출은 `npm run dev:export`(5200)가 워치를 무시하므로 커밋해도 렌더가 안 죽는다 —
 커밋을 미룰 이유가 애초에 없다.)

---

## RESOLVED · "투명 추출이 안 된다" 는 오해 — 알파는 매트가 아니라 빛의 세기 (2026-08-06 종결)

에펙 소재를 뽑다가 "왜 알파로 뽑느냐 / 검정 배경으로 녹화하고 Unmult 로 빼면 되지 않느냐"는
물음이 나왔다. **둘은 같은 연산이다.** 결론부터: 지금 파이프라인은 이미 정상이고,
Unmult 를 써도 결과가 같다(유저가 AE 에서 직접 확인).

**알파의 정체** — `scene.js` grade 패스가 알파를 휘도에서 뽑는다:
```glsl
float L = max(c.r, max(c.g, c.b));
gl_FragColor = vec4(c.rgb, clamp(g * 1.8, 0.0, 1.0));   // 알파 = 휘도 × 1.8
```
AE 의 Unmult 도 똑같이 휘도를 알파로 삼는다. 차이는 **순서뿐**이다 —
셰이더는 8비트로 굽기 전 float 정밀도로, 플러그인은 구운 뒤에. 후자가 반올림을 한 번 더 한다.

**실측(BK_B1 평면 f00115, 2253×3760)**

| 항목 | 값 | 뜻 |
|---|---|---|
| 내용 픽셀 | 1,075,370 | — |
| 완전 불투명 / 반투명 | 505,950 / 569,420 | 절반이 반투명 = 빛의 세기 그라디언트 |
| 평균 `max(RGB)/alpha` | **1.154** | 1.0 이면 프리멀티플라이드 |
| `RGB > alpha` 픽셀 | 416,683 (38.8%) | 프리멀티면 0% 여야 한다 |

→ **프리멀티플라이드가 아니다.** AE 에서 `Premultiplied` 로 읽으면 검정을 빼면서 가장자리가 뜬다.

**에펙 규약(확정)**
- PNG 시퀀스: Interpret Footage ▸ 알파 ▸ **Straight (Unmatted)**
- 레이어 모드: **Add** 또는 **Screen** — 투사는 가산광이라 애초에 매트가 필요 없다.
  그래서 검정 배경본(`preview_black_bg_*`)을 Add 로 얹어도 같은 그림이 나온다.
- Unmult 는 **검정 배경본밖에 없을 때의 보험**. 알파본이 있으면 쓸 이유가 없다.
- 검사할 땐 반드시 **밝은 단색 위**에서 볼 것 — 검정 위에선 검정 제거가 눈에 안 보인다.

**남는 진짜 한계** — 휘도 알파는 어두운 톤을 반투명하게 만든다(회색 머리카락 등).
그래서 인물은 `--layer person`(실루엣 알파)이 따로 있다.
발자국·화살표 같은 순수한 빛 토큰에는 이 한계가 안 걸린다.

측정 도구: `node scripts/measure_alpha_kind.mjs <PNG>`
