# 인수인계 — 2026-08-03 · 인물 룩 파이프라인 통일 + 씬 스테이지 실사 배경

이 문서 하나로 이어갈 수 있게 썼다. 이전 맥락은 `HANDOFF-0802-ALPHA-EXPORT.md`(투명 추출),
`PROJECTION-SPEC.md`(실물 치수), `KNOWN-ISSUES.md`(과거 버그 전말).

---

## 1. 인물 룩 — 공용 규칙으로 묶었다

인물 경로가 **둘**이다. 구조는 같은데 값이 따로 놀아서, 복싱에서 잡은 수정이 러닝·농구에
안 들어가 있었다. `src/main.js` 의 `PERSON_FIELD` 한 곳으로 모았다 — **여기를 바꾸면 모든
인물 뷰에 같이 적용된다.**

| 경로 | 필드 유니폼 | 위치 |
|---|---|---|
| 벽 데모 판(복싱) | `uHeat` / `uHeatN` | `main.js` ~3040 |
| 바닥 코치 판(러닝·농구) | `uField` / `uFieldN` | `main.js` ~2420 |

```js
const PERSON_FIELD = {
  rt: { type: THREE.HalfFloatType },   // ①
  DETAIL_K: 0.45,                      // ②
  detail: () => (FXP.person?.detail ?? 0.42) * 0.45,
  WIDE_STEP: 3.8,                      // ③
};
```

### ① 필드 RT 는 반정밀도여야 한다
8비트로 두면 `detail` 증폭 단계에서 1/255 계단이 몇 배로 벌어져 **밝은 면이 물감 자국처럼
갈라진다**(유저: "부드러운 면이 사라지고 거친 면"). σ·해상도는 안 건드리므로 룩은 안 바뀐다.

### ② 결(detail)은 포화시키면 안 된다
셰이더가 `clamp(uDetail * 2.4)` 라 **0.417 이상이면 좁은 필드 100%** 다. 그 좁은 필드는
320×480 을 1200px 로 3~4배 확대해 쓰는 **비율**(`fN.g / fN.r`)이라, 포화시키면 배·브라처럼
평평한 면에서 저해상 구조가 덩어리로 드러난다(얼룩덜룩 + 과질감). 0.45 배로 눌러 46% 근처.

### ③ 넓은 필드 폭은 룩 슬라이더에서 떼어 놓는다
`1.4 + 2.4 * person.blur` 로 묶여 있었다. blur≈1 을 전제로 쓴 식인데(→3.8), 07-30
"인물 = 뉴턴톤만, 나머지 0" 결정으로 blur 가 0 이 되며 **폭이 1.4 로 주저앉았다.**
좁은 필드가 1.0 이니 두 σ 가 붙고, `detail = 좁음 − 넓음` 이 **DoG(밴드패스)** 가 되어
옷 주름이 아니라 중간주파 압축 노이즈를 골라 증폭한다. '국소 평균'은 정의상 넓어야 한다.

> 코치 판에서 1/4 그리드로 이미 고쳤던 함정이 벽 쪽에만 남아 있었다. 같은 부류가 또 나오면
> **두 σ 가 붙어 있는지부터** 보라.

---

## 2. 잔상 — 필드 갱신 스로틀과 영상 프레임의 어긋남

**증상**: 이전 실루엣 가장자리가 그 자리에 남아 밝게 빛난다(유저: "투명도 프레임이 그 자리에
머물러 빛반사를 계속 이룬다").

**원인**: 본체 셰이더는 매 프레임 **현재 영상 텍스처**로 실루엣 마스크를 뽑는데, 톤·두께 필드만
`if (now - demoLastT < 1/45) return;` 로 45Hz 갱신이었다. 60fps 화면에선 한 프레임 걸러
스킵되어 그 프레임은 **마스크 = 새 자세 / 필드 = 이전 자세**가 된다.

**고침**: 스로틀 폐기. 마스크와 필드가 항상 같은 영상 프레임에서 나온다.

> ⚠ **익스포터에서는 이 버그가 안 보인다.** 가상 시계가 33ms 씩 뛰어 매번 스로틀을 통과하기
> 때문. 그래서 헤드리스 캡처로 "깨끗하다"고 판단하면 계속 틀린다. **실시간 화면에서 봐야 한다.**

같이 껐다: 대류 얼룩(`uNoise` — `uTime` 으로 몸 위를 흘러가 0.x초 주기 얼룩), 복싱 잔상
(`uTrailGain`·핑퐁 RT — 07-30 결정에서 벽만 예외로 남아 있었다).

---

## 3. 씬 스테이지 — 실사 배경 + 촬영 조정

`scenes.html` → `index.html?scene=<ID>` 위에 얹었다. **보면서 맞추고 그 값으로 내보내는** 흐름.

### URL 파라미터
| | |
|---|---|
| `?scene=<ID>` | 씬 고정(기존) |
| `?alpha=1` | 렌더러 알파. **항상 붙인다** — 생성 시점에 정해져서 나중에 못 켠다 |
| `?bg=<url>` | 실사 배경. 3D 실내를 끄고 투사광만 남긴다 |
| `?bgdim=-0.6~0.9` | 음수 = 밝게, 양수 = 어둡게 |

### 런타임 훅 (리로드 없이 즉시 반영)
- `__setSceneBg(url, dim)` · `__setComposite(opacity, blend)` · `__setFp(on)`
- `__sceneAdj` = `{ zoom, pan, tilt, dolly, exposure, bloom, uiX, uiY, opacity, blend, fp }`

배경 파일은 `public/_bg/` 에 두고 `scenes.html` 의 `BGS` 배열에 한 줄 추가.

### 뺀 것 — UI 크기 슬라이더
투사 크기는 프로젝터 광학·설치 거리에서 **계산되는 값**이다(`computeStation()`).
메시만 늘리면 그 계산을 우회한 거짓말이 된다. 크기를 바꿔야 하면 투사 폭(`s-wallw`)으로
스펙 자체를 바꿔야 벽거리·틸트·사람거리가 같이 갱신된다.

---

## 4. 반복해서 밟은 함정 (전부 실제로 밟았다)

1. **"한 번만 설정"은 이 앱에서 안 통한다.** 앱이 매 틱 자기 값으로 되돌린다.
   무대 격리·배경 지우기·카메라 고정 전부 **매 프레임 다시 걸어야** 한다.
2. **`document.querySelector('canvas')` 는 타임라인 UI 캔버스를 먼저 잡는다.**
   WebGL 캔버스는 `renderer.domElement`, 없으면 가장 큰 캔버스를 골라라.
3. **`mix-blend-mode` 는 같은 스태킹 컨텍스트 안에서만 섞인다.** 배경을 body 에 깔면
   캔버스는 중간 컨테이너와 섞여 "가산이 안 먹는다". 배경은 **캔버스 바로 부모**에.
4. **렌더 중 리포에 파일을 쓰면 vite 가 새로고침해 렌더가 죽는다.** 17분짜리를 두 번 날렸다.
5. **정지 프레임 한 장으로 시간축 아티팩트를 판단하지 마라.** 잔상·깜빡임은 프레임 간
   변화량으로 재야 한다(`scripts/measure_flicker.mjs`). 오늘 이걸로 여러 번 오판했다.
6. **다른 조건에서 잰 성능값을 옮겨 쓰지 마라.** 씬마다 프레임당 시간이 2배씩 차이 난다.
7. **작업 전 `git fetch`.** 로컬이 23커밋·11커밋 뒤진 채로 작업해 두 번 헛돌았다.

---

## 5. 실측 성능표 (씬 스테이지 BX_READY 기준)

| 설정 | 렌더 면적 | 프레임당 | 8초(240f) |
|---|---|---|---|
| `--w 1280 --ss 2` (출력 1280×720) | 2560×1440 | 1.08초 | 4분 18초 |
| `--w 2560 --ss 1` (출력 2560×1440) | 2560×1440 | 1.77초 | 7분 04초 |
| `--w 3840 --ss 1` (출력 3840×2160) | 3840×2160 | 4.23초 | 약 17분 |

- **`--ss N` 은 N배로 렌더한 뒤 `--w` 크기로 되돌린다.** PNG 시퀀스만 큰 해상도로 남고
  `.mov` 는 `--w` 크기다. 4K `.mov` 가 필요하면 `--w 3840 --ss 1`.
- `--uiscale` 은 4K 에서 병목이 **아니었다**(2 → 1.0 으로 내려도 4.41 → 4.47초, 차이 없음).
- GPU 절벽은 **12MP 부근**. 4K(8.3MP)는 그 아래라 통과하지만, `--pad` 를 얹으면 넘어간다.

---

## 6. 소스 클립 규격

`docs/ghost-clips.md` 가 정본. 오늘 확인한 것만 덧붙인다.

- **해상도**: 씬 스테이지 4K 출력이면 1080×1920 로 1.35배 여유. 평면 대지 4K 는 0.95배로
  아슬하다. `yuv420p` 는 색 해상도가 절반이라 크로마키 경계는 4K 소스가 눈에 띄게 깨끗하다.
- **fps**: 소스 24fps 를 29.97 로 뽑으면 **불규칙 프레임 반복(저더)** 이 생긴다. 해상도보다
  이게 먼저다. 출력을 24 로 맞추거나 소스를 30fps 로 받아라.
- `public/ghost/<파일>` 에 넣으면 코드 수정 없이 반영. 스테이지 매핑은 `GHOST_CLIPS`.
  **BX_READY 는 전용 클립**(`bx_ready_guard.mp4`) — 예전엔 B1 과 공유해서 첫 장면만
  바꾸려 해도 B1 까지 바뀌었다.

---

## 7. 남은 것

- **바닥 팩(러닝·농구) 인물 시각 검증** — `PERSON_FIELD` 적용은 코드로 확인했으나, 프로브
  대기(12초)가 짧아 장면이 안 올라와 눈으로는 아직 못 봤다. 실시간 화면에서 확인 필요.
- **4K 렌더가 121/240 에서 puppeteer 오류로 죽은 건** 원인 미확인. 재현되면 파야 한다.
- 씬 스테이지 `setHex` 페이지 에러 3건(농구·러닝) — 기존 이슈로 보이나 확인 안 함.
- 익스포터가 `__sceneAdj` 값(zoom/pan/tilt/exposure/bloom)을 아직 안 받는다. 화면에서
  맞춘 값을 그대로 내보내려면 연결이 필요하다.
