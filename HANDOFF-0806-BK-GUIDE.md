# HANDOFF 08-06 — 농구 스텝백 가이드 재편 + 주목 강조 룩

커밋 `cfbfd33` → `66fbc1e` (브랜치 `b2-slip-clip-rebuild`, 푸시 안 함).

---

## 0. 먼저 읽을 것 — 이 리포에서 앱을 통째로 죽이는 함정

`src/fx-core.js` 와 `src/tokens.js` 의 GLSL 은 **JS 템플릿 리터럴**이다.
**주석에 백틱을 쓰면 문자열이 거기서 끊기고**, 뒤쪽 셰이더가 JS 로 파싱돼
`SyntaxError` 로 모듈이 죽는다. 그러면 영상도 봇도 링도 **아무것도 안 뜬다**
(증상이 "셰이더가 이상하다"가 아니라 "화면이 통째로 빈다"라서 엉뚱한 데를 판다).

08-06 에 실제로 두 번 났다(`inFill`, `assets/icons/...`). 편집 후 반드시:

```
node --check src/fx-core.js && node --check src/tokens.js && node --check src/main.js
```

---

## 1. 무엇이 바뀌었나

### 1-1. 스텝백을 실측으로 다시 쪼갰다 (4조각 → 3조각)

클립 `public/stepback_fwd.mp4` (760×637, 3.33s, 30fps) 를 MediaPipe 33관절로
100프레임 전부 쟀다. 발목 x 속도(/초):

```
0.00~1.167  L 0.598 · R 0.765 에서 완전 고정   제자리 드리블뿐 (발이 아무 데도 안 간다)
1.20~1.47   R 0.763→0.529  최고 2.88          ① 오른발 크로스 딛기
1.50~1.70   L 0.598→0.177  최고 5.35          ② 왼발 뒤로 빼기
1.80~2.10   R 0.503→0.239  최고 1.93          ③ 오른발 모으기
2.13~3.33   전 관절 정지                       데드 1.2초
```

- 구값 `1/4 무릎 굽혀 페이크(0~0.60)` 는 **발이 안 움직이는 구간**이었고, 엉덩이는
  오히려 올라간다(0.612→0.674) — 큐와 화면이 반대였다.
- 구값 `4/4 끝 3.10` 은 뒤쪽 1.2초가 **정지 프레임**이다. 배우는 사람이 멈춘 화면을 봤다.
- 반대로 유저가 감으로 찍었던 `1.44 / 1.81` 은 실측 경계와 거의 정확히 맞았다.

정본: `src/session.js` `STEP_SEG` — 끝값 하나가 아니라 **`[시작, 끝]`** 이다.
`SB_POSE` 키 시각도 실측에 맞췄다.

### 1-2. 농구 스테이지 13 → 11

```
BK_READY → BK_A1(옆구리) → BK_B1(드리블) → BK_T1(전체 재생 ★신규 역할)
         → BK_B2 · BK_B3 · BK_B4(3조각) → BK_T2 → BK_C1 → BK_C2 → BK_FIN
```

- `BK_A3` 스쿼트 **삭제** — 배우는 동작이 아닌데 혼자 다른 UI 한 벌(중앙 링 + 깊이
  아크 + 중앙 카운트)을 끌고 있었다. ⚠ **핸들러(`bkSquat`)와 빌더는 아직 안 지웠다** —
  스테이지 목록에서만 빠져 도달 불가 코드로 남아 있다(§4).
- `BK_B5` **삭제** — 그 구간 뒤쪽이 정지 프레임이라 배울 게 없었다.
- `BK_T1` 은 빈 전환 4.5초였다 → **전체 재생**(0.5배속 2.3초). 새 스테이지를 안 늘렸다.
  관찰 전용이라 렙이 아니라 **재생 1회**(`_pvLoops`)로 끝나고, 셀 게 없으니 링도 안 켠다.

### 1-3. 주목 강조 룩

**따라다니지 않는다.** 동작이 시작될 때 자리 하나가 생기고, 그 자리는 **도착 지점에 고정**이다.
(첫 시도는 발을 매 프레임 따라다녔는데, 그러면 시선이 늘 뒤늦고 '움직이는 얼룩'만 남는다.)

비트 표는 속도 문턱(0.35/초)으로 자동 검출한다 — 손으로 안 찍는다:

```
R1 오른발  1.200 → 1.433  지점(0.534, 0.167)  "오른발"
L  왼발    1.500 → 1.700  지점(0.185, 0.107)  "왼발"
R2 모으기  1.800 → 2.067  지점(0.210, 0.107)  "모아"
```

색은 **하드코딩 2단 램프**다(`fx-core.js` `personGuideColor`). LUT 대역(uPHi)을 미는
간접 방식은 '얼마나 연해지나'가 안 잡혀서 버렸다:

```
연(전신)  sand #FEC389 → 거의 흰빛      = 맑은 코랄 / 연연한 주황
진(강조)  red  #FA3030 → coral #FE6E3C  = 뉴턴 메인컬러로 찐해진다
```

⚠ 네 색 전부 R≈1.0 이다. 투사광 알파 불변식(`알파 = min(aOut, lum×1.6)`)에 안 걸린다 —
**색을 바꾸려면 이걸 먼저 확인할 것.** 어두운 색을 넣으면 그 픽셀만 투명해져 뒤 바닥이 배어 오른다.

토큰은 dab2n `setup-injury` 의 `assets/icons/body-ring.png` **그 파일 그대로** 쓴다.
픽셀로 재 보니 부드러운 글로우가 아니라 딱딱한 2톤 원반이다:

```
r/R 0.00~0.36  #FFFFFF 순백 코어(불투명)  ·  0.36~0.40 좁은 전이
r/R 0.40~0.94  #FFC494 주황 링           ← 팔레트 sand(#FEC389)와 사실상 같은 색
r/R 0.94~1.00  밝은 테두리 + 알파 255→96
CSS drop-shadow(0 2px 8px rgba(250,48,48,.45))  ← 뉴턴 RED 그대로
```

글로우로 흉내 내면 이 딱딱한 경계가 사라져 '토큰'이 아니라 '빛번짐'이 된다.
크기도 원본에서 역산했다: 원본 38px / 인물 470px = 키의 8% → 이 판에서 반경 0.025.
(처음 쓴 0.075 는 3배였고 실제로 발을 통째로 덮었다 — 렌더로 확인하고 고쳤다.)

### 1-4. a2Cyc 누수 (별건)

`session.a2Cyc` 를 A2 밖에서 아무도 안 지워서, A2 가 끝나도 `{prog:1, holdSec:3}` 이
세션 끝까지 남았다 → 마크가 Success(빨강)·숫자 1 인 채로 영영 굳었다.
`main.js` 에서 **매 프레임 A2 밖이면 null** 로 지운다(`demoActive` 와 같은 규약).

---

## 2. 파일 지도

| 무엇 | 어디 |
|---|---|
| 구간표 정본 `[시작,끝]` | `src/session.js` `STEP_SEG` |
| 발자국 포즈 키 | `src/session.js` `SB_POSE` |
| 스테이지 목록 | `src/session.js` `STAGES.basketball` |
| 알약/링/진행 규약 | `src/floorgl.js` `CAPS` · `ADV` |
| 발 문법(load/move/enter/exit) | `src/marklang.js` `BK_STEPBACK` |
| 코치 판 규격 | `src/coach-cfg.js` |
| 지면 씬 타이틀 | `public/ready-view/floor-scenes.js` |
| 가이드 셰이더 | `src/fx-core.js` `PERSON_GLSL` (`personGuideColor` · `hotAt` · `gazeToken`) |
| 강조 주입 | `src/main.js` `setHotspot` · `beatAt` · `tickHotVoice` |
| 비트 표(생성물) | `public/stepback-hotspots.json` |
| 랜드마크 원본 | `assets/mocap/stepback_fwd-landmarks.json` |

### 다시 굽는 법 (영상이 바뀌면)

```
node scripts/extract_stepback_landmarks.mjs   # 영상 → 33관절 정규좌표 (vite 5199 필요)
node scripts/build_stepback_hotspots.mjs      # 랜드마크 → 비트 표
```

`STEP_SEG` 의 구간도 첫 스크립트 출력의 속도표를 보고 다시 정한다.

### 눈으로 확인하는 법

```
node scripts/shot_guide_look.mjs   # → tmp_guide_look.png
```

코치 판과 **같은 셰이더 조각**에 실제 클립 프레임(`public/_gframes`)을 물려 렌더한다.
⚠ **헤드리스에선 앱 화면으로 못 본다** — 코치 mp4 가 `readyState 2` 를 못 넘어서
인물·발마크·파문이 아예 생성되지 않는다. 이 제약은 이번 변경 전 HEAD 에서도 같다.
그래서 실물 확인은 브라우저에서 해야 한다.

---

## 3. 조절 노브

```js
FXP.hot = { on: 1, pale: 0.97, hot: 0.55, k: 1, gaze: 1 }
```

- `pale` ↑ 전신이 더 하얘짐 · `hot` ↓ 강조가 더 쨍해짐(대역 상단이 낮을수록 붉다)
- `on: 0` 이면 기능 전체 꺼짐 = 도입 전과 픽셀 동일 (롤백 지점)
- 재생 배속: `stepRate` — 학습 0.5배 / 실전 `BK_C2` 1.0배 · 구간 끝 정지 `stepHold` 1초

---

## 4. 남은 일 (미해결 — 여기서부터 이어받으면 된다)

### ① 음성 파일이 없다

`public/voice/say_sb_beat_{R1,L,R2}.mp3` — "오른발" / "왼발" / "모아".
없으면 브라우저 TTS 로 떨어진다(이 리포가 피하려는 그 기계음).
키는 고정해 뒀으니 파일만 넣으면 그때부터 그게 나간다.
⚠ 비트 간격이 **0.60초**다. 한 단어를 넘기면 `speak()` 가 앞 음성을 pause 해 서로 잘린다.

### ② 파형이 투사면 밖으로 새는 문제 — **원인 미확정**

유저 신고(스샷: `CROSS STEP 1/3` 화면에서 주황 선이 빔 사다리꼴 밖으로 뻗음).

검수 스크립트를 만들어 뒀다: `node scripts/audit_beam_leak.mjs`
**하지만 헤드리스에선 결론이 안 난다.** 두 가지 이유로:

1. 코치 영상이 안 떠서 발마크·파문이 **생성 자체가 안 된다**(지면 메시가 4종만 잡힘).
2. 헤드리스에서 `rig._fp` 가 **null** 이다(`projector.update()` 가 `getKneeWorld()` null 로
   조기 return 하는 것으로 보임). `_beamFade`(오브젝트 알파)도 `footprintFade`(프래그먼트)도
   **둘 다 `rig._fp` 가 없으면 통째로 건너뛴다** — 그래서 "전부 샌다"로 나오는데 그건 환경 탓이다.

**브라우저에서 다음 한 줄이 판정한다:**

```js
__dbg.rig._fp
```

- `null` → 클램프가 아예 안 도는 것. 그게 유출 원인이고, `projector.update` 의 조기 return
  조건부터 본다(`projector.js:308~316`).
- 객체가 나옴 → 클램프는 도는데 **커지는 것**만 샌다. 구조적 이유가 있다:
  `_beamFade`(`session.js:1145`)는 **오브젝트 중심 하나**로 알파를 정한다. 반경이 자라는
  링·파문은 중심이 빔 안에 있는 한 절대 안 걸리고, 가장자리는 밖으로 나간다.
  → 해법은 그 재질에 **프래그먼트 페이드**(`footprintFade`, `tokens.js:37`)를 물리는 것.
  마크·레인 재질엔 이미 `uFPNear` 계열이 있고 `session.js:2275` / `2315` 에서 주입한다.
  파문 재질(`effects.js:189`)은 `it.isFloor && !it.noClip` 일 때만 주입한다 — **이 조건에서
  빠지는 항목이 있는지부터 본다.**

### ③ 농구 구경 뷰에서 아래가 잘린다 — 착수 안 함

유저: "조금 더 앞에 투사하든 고개를 더 기울이든 해서 잡히게 해, 아래가 잘려."
같은 스샷에서 발자국이 화면 **하단 경계**에 물려 있다. 후보 두 개:
- 콘텐츠를 앞으로: `coach-cfg` 의 `fwd`, 또는 `SB_BOX.v0/v1`(`session.js`)
- 카메라: 구경(1인칭) 뷰의 피치

②와 **같은 투사 창 좌표계**를 건드리므로 ②를 먼저 확정하고 함께 보는 게 낫다.
크기 판단은 감으로 하지 말 것 — `docs/FLOOR-CONTENT-PLACEMENT.md` 와
`docs/FLOOR-LEGIBILITY.md`(시각도 역산)가 근거다.

### ④ BK_A3 죽은 코드

`bkSquat` 빌더·핸들러(`session.js:1441` 부근 · `2909` 부근)와 `BK_REPS.BK_A3` /
`BK_SQUAT_REPS` 가 남아 있다. 스테이지 목록에서 빠져 **도달 불가**다. 지워도 되지만
한 번에 지우면 진단이 어려워져서 이번 커밋에선 남겼다.

---

## 5. 검증 상태 (뭘 확인했고 뭘 못 했나)

확인함:
- 농구 11스테이지 순회 — 페이지 에러 0건, 순서 정상
- 셰이더 컴파일 0에러 · 비트 표 로드 확인
- `check_mark_lang` 실패 5 → 4 (BK_A3 소멸분) · `check_content_fit` 5건은 전부 기존 것
  (A3·BK_A2 인물 높이, z밴드 — 이번 변경과 무관)
- `BK_T1` 인물 실높이 0.548m = 기준의 100%
- 룩은 `shot_guide_look.mjs` 렌더로 육안 확인 (전신 연함 → 비트에서 그 부위만 진해지고 토큰이 붙음)

**확인 못 함:**
- **앱 화면에서의 실제 영상 재생** — 헤드리스 `readyState` 제약(위 §2). 브라우저 확인 필요.
- **②·③** — 위 참조.
