# 인수인계 — 2026-08-07 · A2 종아리 늘리기 검정배경 평면 녹화

**목표(유저)**: A2(종아리 늘리기)를 **검정배경 · 평면(직교) · 잉크(Normal)** 로 녹화한다.
블렌드 후처리는 유저가 에펙에서 아주 조금만 한다. "자잘한 디자인 오류도 고치고 하드코딩해서라도
완벽하게" 뽑는 것이 목적이고, 타이밍·배치는 **실사 레퍼런스 실측**에 맞춘다.

레퍼런스: `~/Desktop/발누르기이브게.mov` (1794×1016 · 60fps · 9.005s · 540프레임).
뉴턴 데모 몽타주의 화면녹화다 — 좌 패널 1인칭 트랙, **우 패널이 실사 종아리 스트레칭**.
자막 "Bend your front knee. Keep your back leg straight." · 7.95s 에 컷.

---

## 1. 확정 실측값 — 이 표가 정본이다

### 타이밍 (빨간 신발 연결요소 추적 · 축척 4.35mm/px)

```
홀드              2.15s    뒷발(늘어나는 종아리)에 3·2·1
앞발 당겨오기      0.35s    앞발만 뒤로 (499→293px) · 뒷발은 제자리
모아 섬           1.25s    두 덩어리가 하나로 겹침
반대발 내딛기      0.55s    반대 발이 앞으로 (355→594px)
────────────────────────
1렙               4.30s    실사 홀드→홀드 간격과 같다
```

**★ '내려가기'는 따로 있는 동작이 아니다.** 발을 내미는 그 0.55초에 자세가 같이 낮아진다.
처음에 상의 높이만 보고 `{내려가기 0.55 · 홀드 2.15 · 일어서기 0.50 · 교대 1.05}` 로 갈랐는데,
같은 동작을 둘로 센 것이었다. 발 추적이 더 직접적인 신호다.

**★ 발 교대는 좌우 반전이 아니다**(유저: "좌우 반전인 게 말이 안 되잖아").
사람은 미러로 뒤집히지 않는다. 앞발이 돌아와 모이고 → 모아 선 채로 있다가 → 반대 발이 나간다.
회차 파리티로 스왑하지 말 것. 발이 실제로 이동하면 역할은 저절로 바뀐다.

### 스탠스

```
앞뒤 1.54m   (홀드 127프레임 중앙값 353px × 4.35mm/px · 러닝화 270mm 기준)
좌우 0.20m   ← 실측 아님. 측면뷰라 횡폭은 원리적으로 못 잰다. 눈으로 맞추는 노브다.
```

지금 시뮬은 `x ∓0.16 · z 동일` = **좌우 0.32m · 전후 0**. 런지가 아니라 거의 모아 선 자세다.

> **⚠ 재측정 함정 — 처음에 1.19m 로 잘못 쟀다.**
> `crop=iw/2:...` 의 실제 폭이 **896** 인데 897 로 파싱해서 프레임마다 한 픽셀씩 밀렸다(사선 찌그러짐).
> rawvideo 는 폭을 안 알려 준다. **반드시 바이트 수로 검산할 것**:
> `n % (w*h*3) === 0` 인 (w,h) 를 찾거나 `ffprobe -f lavfi -i "movie=…,crop=…"` 로 물어본다.
> 이 실수는 에러 없이 그럴듯한 숫자를 낸다 — 가장 나쁜 종류다.

### 트랙 바닥 색 (`public/floors/track.jpg` 실측)

```
표면    #9FA58F  휘도 160/255 (63%)
레인선  #D5D6D0  휘도 214      면적 5.1%
```

| 색 | 가산 결과 | 채도 | 잉크(Normal) | 채도 | 휘도차 |
|---|---|---|---|---|---|
| #FA3030 | #ffd5bf | 25% | #fa3030 | 81% | −52 |
| #FF4000 | #ffe58f | 44% | #ff4000 | 100% | −47 |
| #FF8E5E | #ffffed | **7%** | #ff8e5e | 63% | **+10** |
| #FF3300 | #ffd88f | 44% | #ff3300 | 100% | −55 |

**밝은 바닥에서 가산은 빨강을 살구빛으로 바래게 한다.** 더할 여유가 37%뿐이고 흰 레인선 위는
**16%** 다 — 마크가 레인선을 가로지르면 그 구간만 사라진다. 그래서 **잉크로 뽑는다**(유저 확정).
`footlab.html` 자체 주석도 같은 결론에 이미 도달해 있다("세기가 아니라 잉크로 모델링이 답이다").

**Preview 가 쓰는 #FF8E5E 는 잉크로도 휘도차 +10뿐** — 바닥과 밝기가 붙어 흐리게 읽힌다.
밝은 면에서 대비를 만드는 방향은 위가 아니라 아래다. (미결)

---

## 2. 이미 들어간 것

### `footlab.html` — A2 카드 (바닥 합성 카드 바로 아래)

실제 트랙 사진 위에서 실측 스탠스·실측 타이밍으로 돈다. 5199 에서 열면 된다.
세그로 **실측 1.54m ↔ 지금 시뮬 0.32m** 를 즉시 바꿔 본다.

- `A2T` / `A2_CYC` / `A2_STANCE` — 위 실측표가 그대로 상수로 있다.
- `a2Seg(t)` — 구간·진행률. `a2Feet(S)` — **두 발의 앞뒤 위치**(smoothstep, 파리티 스왑 없음).
- `feedFloorMark(wm, ph, prog, t, mode)` — 합성 카드와 A2 카드가 **한 벌**로 쓰는 유니폼 공급기.
  같은 블록이 두 벌 있던 것을 뽑았다. 슬라이더를 추가하면 여기만 고치면 된다.
- `window.__flab.a2t` / `.A2_CYC` / `.A2T` — 추출 하니스가 위상을 겨냥하게 연 훅.

**★ A2 카드만 평면을 `FOOT_PLANE_M * QUAD_K`(=0.619m) 로 짓는다 — 이 카드는 진짜 미터다.**
걷기 카드는 `0.86 * QUAD_K` 라는 임의 단위를 쓴다(1단위 ≈ 0.48m). 거기에 실측 미터를 그대로
꽂았더니 좌우 0.20 이 실제로는 0.096m 가 되어 **두 발이 겹쳤다**(유저 스샷). 걷기 카드는
유저 검수를 지난 크기감이라 건드리지 않았다.

### `scripts/export_video.mjs`

- **부팅 리로드 내성**(`49bfe1d`). `warm()` 에서 실행 컨텍스트가 날아가면 다시 붙어 워밍업을
  재시작하고 `reloaded` 를 0 으로 되돌린다(안 그러면 프레임 루프가 즉시 중단).
- **`--ramp "<소스초>:<배속>,…"`** — 시간 구부리기. 출력은 등간격이고 **소스 시각만** 민다.
  그 시각을 진짜로 렌더하므로 에펙 리타이밍의 보간이 없다(0.2배속도 안 뭉갠다). `--dur` 은 출력 길이.
- **자원 계측** — 죽는 프레임에서 `renderer.info` 를 같은 왕복으로 받는다(추가 비용 0).

### `public/floors/track.jpg`

레퍼런스 트랙 사진. footlab 이 자동으로 집어 쓴다(없으면 근사색으로 그린다).

---

## 3. 아직 안 넣은 것 — 시뮬 타이밍 5곳

**다른 세션이 같은 파일들을 쓰고 있어서 손대지 않았다.** 아래가 그대로 패치 목록이다.

| 파일 | 고칠 것 |
|---|---|
| `src/marklang.js` | `STAGE_TIME.A2.hold` 3.0 → **2.15** |
| `src/main.js:4497` | 화면시간/클립시간 분리. 지금은 세션 시간이 모캡 클립 시간에 **1:1** 로 물려 있다(`T0 5.4 · TD 6.5 · T1 8.1`). 당겨오기 0.35 · 모아 섬 1.25 · 내딛기 0.55 로 재구성하고 **파리티 미러(`xbot.group.scale.x`) 를 없앤다**. `_a2LoopNeed` = `A2_WATCH + 2*4.30` |
| `src/floorgl.js:1889` | `Math.ceil(hs*(1-hp))` → `a2Rem(hp)` |
| `src/session.js:2820` | 마크 안 숫자도 같은 이유로 `a2Rem` |
| `src/session.js:1357` | 초기 라벨 `round(hold)` → `a2Rem(0)`. 지금대로면 홀드 2.15 에서 **'2'** 가 떠 있다 |

**3·2·1 은 홀드가 2.15초여도 다 보인다.** `src/a2hold.js` 의 `a2Rem` 이 이미
`ceil(3*(1-hp))` = **진행률 3등분**(각 0.717초)이다. 남은 초로 올림하면 "3" 이 0.15초만
번쩍하고 사라진다 — 위 세 곳이 그 초 기반 계산을 아직 쓰고 있다.

---

## 4. 열려 있는 것

**① 홀드에서 '칼같이 잘린다'(유저)** — 확대하면 마크 오른쪽에 직선 경계가 보인다.
각인(`uImp`/`uImpOff`)이 쿼드 밖으로 밀려 잘리는 것으로 **의심**되지만 **미규명**이다.
이분 탐색(`tmp_bisect_cut.mjs`: `offx/offy=0`, `imp=0`, `rip=0`, `pool=0` 을 하나씩 끄고
세로 직선 끊김 길이를 잰다)을 만들어 뒀는데 계측 상자가 캔버스 밖으로 나가 0 만 나왔고
(상자 수정함: `430,300,320,300` · 뒷발 중심 ≈ `585,506`), 재시도가 리로드로 죽었다.
**추측으로 고치지 말 것 — 끄면 사라지는지가 증거다.**

**② 타이머를 발에 붙이기(유저)** — "원래 타이머 발에 있기로 한 거 아냐? 종아리 풀기면 뒤에 있던 거."
정본 규약과 같다: 링·카운트·Success 는 전부 **뒷발**(`a2Cyc.workLeft`, 늘어나는 종아리 쪽).
지금 랩 카드는 숫자를 안 그리고 상단 크롬 텍스트로만 보여 준다. `drawNumber`(fxlut) +
`MARK_NUM.anchor(a, right, FOOT_PLANE_M * QUAD_K)` 경로로 붙이면 된다(tokens.js:950 참고).

**③ WebGL 컨텍스트 손실** — 긴 렌더가 중간에 죽는다. 원인은 **갈라 뒀다**:
120프레임 내내 `geo 19 / tex 18 / prog 47` 미동 없음 = **누수 아님**. 900px 로 낮춰도
86프레임에서 죽음 = **상한도 아님**. 남은 것은 지속 부하 — 프레임당 시간이 1.00s → 1.65s 로
늘어나는 것이 같은 얼굴이다. 청크로 나눠 렌더하고 이어 붙이는 것이 유력한 우회로다
(`--t0` 로 구간을 나누면 `--play` 대기 로직이 이미 그 지점까지 기다려 준다).

**④ A2 씬 저장본이 없다** — `public/_presets.json` 에 `P1` 하나뿐이다. `scenes.html` 에서
'이 씬 저장'을 해야 카메라·노출·블룸·색보정이 녹화에 실린다. 안 하면 전부 기본값으로 간다
(익스포터가 크게 경고한다).

**⑤ 동시 세션** — 이 리포에 두 세션이 붙어 있었고 서로의 작업을 `git add -A` 로 쓸어 담았다.
A2 판 190줄·`track.jpg` 는 `2cb01c6` 에, `__flab.a2t` 훅은 `80948c7` 에, 발 교대 수정은
`47c5881` 에 각각 **남의 메시지를 달고** 들어갔다. 더 나쁜 건 렌더 중이면
**vite 가 src 저장마다 full-reload 를 때려** 516프레임 렌더가 첫 12초에 죽는다는 것이다.
**긴 렌더를 돌릴 때는 다른 세션이 src 를 저장하지 않아야 한다.**

---

## 5. 녹화 명령

```bash
# 검정배경(기본) · 평면 직교 · 잉크. --alpha 를 주면 투명이 되니 주지 말 것.
node scripts/export_video.mjs --scene A2 --flat --play --t0 0 --dur 17.2 \
  --fps 30 --w 1600 --ss 1 --out out/A2

# 시간 구부리기 예: 홀드만 0.3배속
node scripts/export_video.mjs --scene A2 --flat --play --ramp "0:1,2.15:0.3,4.30:1" …
```

`--ss` 는 1600 이상에서 1 로. 지면 대지는 1600×2670 이라 `--w 1600` 이 등배다.

## 6. 하니스 (tmp_ 는 gitignore 라 사라진다 — 필요하면 다시 만들 것)

| 스크립트 | 하는 일 |
|---|---|
| `tmp_qa_a2.mjs` | A2 카드 잉크 화소 % + 콘솔 에러 |
| `tmp_grab_a2.mjs` | 한 사이클을 N장으로 추출(위상 정확). `performance.now` 를 갈아 끼워 겨냥한다 |
| `tmp_shot_a2seg.mjs` | 구간별 한 장씩 |
| `tmp_probe_a2scale.mjs` | 월드 단위가 미터인지 역산 |
| `tmp_bisect_cut.mjs` | 칼같이 잘림 이분 탐색(위 ①) |

전부 `waitUntil: 'domcontentloaded'` 를 쓴다 — `networkidle2` 는 다른 세션이 저장 중이면 영영 안 온다.
