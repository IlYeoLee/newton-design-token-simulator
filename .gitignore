node_modules/
dist/
.DS_Store
*.log
assets/models/*.task
tmp_*.jpg
tmp_*.mjs
tmp_*.js
tmp_*.png

# 내보낸 영상 — 용량이 커서 저장소에 넣지 않는다
out/

# 실사 배경 영상 프록시 — 수백 MB. make_bg_proxy.mjs 로 언제든 다시 만든다
public/_bg/*.mp4

# Claude Code 개인 설정(자동 커밋 훅) — 공유하지 않는다
.claude/settings.local.json

# 배경 원본 백업 — 보정 전 파일. 원본은 git 히스토리에도 남으므로 리포엔 안 넣는다.
public/_bg/_orig/
