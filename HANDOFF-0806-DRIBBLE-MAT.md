# 인수인계 — 드리블 매트 토큰 (BK_B1)

**작업일** 2026-08-06
**발단** 유저: "농구공 튀길 때 정말 둥근 원 1개만 보여줘도 되나? 가이드로서 성의가 없어 보인다."
**결론** 실물 농구 스텝 매트를 지면 투사 문법으로 옮긴 **영역 토큰**을 새로 만들고 BK_B1 에 넣었다.

---

## 1. 무엇이 생겼나

| 파일 | 내용 |
|---|---|
| `src/fx-core.js · drawDribbleMat` | **새 프림 토큰**. 외곽·브래킷·눈금자·번호 표적·액티브 타깃·커넥터·셰브론·워드마크 |
| `src/session.js` | `PRIM_DEFAULTS.dribbleMat` 등록 · `tickPrims` 분기 · `bkB1.mat` 인스턴스 |
| `fxlab.html` | 룩 랩 프림 갤러리에 편입(`MAT_DEMO` 데모 구성 + 슬라이더 4종) |
| `public/textures/bk_dribble_mat_D.png` | 합성용 판 — 1 px = 1 mm, 알파 투명 |
| `docs/bk-b1-dribble-mat.html` | 작도 초안(치수·빔 창·피그마 프레임 좌표) |
| `docs/bk-b1-dribble-mat-look.html` | 룩 시안(1인칭 렌더·네 가지 안·주간 광량 예산·합성 규격) |
| `scripts/_probe_b1mat.mjs` | 빔 창·대지 밴드·요소 좌표 **실측** 덤프 |
| `scripts/_shot_b1mat.mjs` | 시뮬 BK_B1 캡처 |
| `scripts/_shot_mattoken.mjs` | 토큰 단독 평면 프리뷰 |
| `scripts/export_mat_png.mjs` | 합성용 PNG 내보내기 |

전부 `npx vite --port 5199` 를 띄운 상태에서 돌린다.

---

## 2. 왜 사각형이 아니라 사다리꼴인가

투사 반폭이 앞거리에 비례해 자란다 — 이게 이 판의 형태를 정한 유일한 이유다.

```
    반폭(d)      = 0.32 + 0.27 × (d − 0.30)        projector.js _halfAt
    100% 반폭(d) = 반폭(d) − 0.15                   tokens.js uFPFadeM (마크 셰이더 페더)
```

사각으로 그리면 가까운 쪽 두 귀퉁이가 페더에 잠겨 스러진다. 그래서 매트 외곽은
**빔 창의 100% 밝기 경계**에 그대로 앉힌다. `session.js` 의 `matInk(d)` 가 그 식이다.

깊이 범위도 계산에서 나온다.

```
    앞(가까운 변) d 0.45 = fpNear(0.30) + 페더(0.15)
    뒤(먼 변)     d 1.22 = 대지 CONTENT 밴드 상단(y1007) — 더 가면 타이틀 알약 자리
```

**발끝 기준**으로 환산하면(발끝 = 몸 축 + 0.15 m):
매트는 발끝 **0.30 m 앞에서 시작해 1.07 m 앞에서 끝난다**. 링은 0.85 m 앞.
발 밑에는 못 깐다 — 무릎에서 쏘니 자기 정강이에 가린다. 실물 매트와 결정적으로 다른 점이고,
스탠스 표적 ①② 가 **먼 쪽**에 있는 이유다(가까운 쪽은 좁아서 어깨너비가 안 들어간다).

---

## 3. 토큰 파라미터

```js
P = {
  mat:     { nx, fx, ny, fy },   // 판 정규좌표(-1..1, +y = 먼 쪽). nx = fx 면 직사각
  targets: [{ x, y, n, r, on, v? }],
  center:  { x, y, r, label, ring },   // ring:0 = 링은 3D 존 마크가 그린다
  ruler:   { w, h },             // 실치수(m) — 눈금·라벨이 여기서 나온다
  title, brand, chev, bracket, round, prog,
}
```

지켜야 할 규약 둘:

- **색 = 상태다.** 표적 네 개는 같은 색조를 쓰고 `on` 만 밝기로 갈린다. 정체성은 번호가 말한다.
  (초안에서 실물처럼 4색 코딩을 했다가 팔레트 규칙 ②와 어긋나 되돌렸다.)
- **치수는 손으로 적지 않는다.** `session.js` 는 `matInk(d)` 와 밴드 상수에서 판을 계산한다.
  숫자를 바꾸고 싶으면 `MAT_D0` / `MAT_D1` 만 만지면 폭이 따라온다.

---

## 4. 지금 상태 · 다음 사람이 이어서 할 일

- **① ② 만 점등, ③ ④ 는 자리만 잡고 꺼져 있다.** 사이드 드리블 단계에서 `on: true` 로 켜면
  같은 판을 재사용한다 — 단계마다 새 그래픽을 그리지 않는 게 이 설계의 요점이다.
- **미해결 — 매트 아래 절반이 시뮬 카메라 밖이다.** 링(d 1.0)이 이미 화면 최하단이라
  ③ ④ 와 가까운 쪽 브래킷은 안 보인다. 카메라는 임의로 안 건드렸다.
  ㉮ 시선 각도를 내리거나 ㉯ 매트 깊이를 줄여 위로 당기거나 — **유저 결정 대기**.
- **프림 판은 빔 마스크를 안 탄다.** `primPanel` 재질은 `beamAlphaAt` 을 안 쓴다.
  지금은 판을 100% 영역 안에 맞춰 놔서 문제가 안 되지만, 판을 키우면 창 밖에도 그려진다.
- **주간 야외 가시성**은 광량 문제다. 선 스캐닝 기준 그늘 210 lm · 직사광 1,400 lm.
  면 투사로는 5만 lm 이라 불가능. 근거와 전제는 룩 시안 문서에.

---

## 5. 확인하는 법

```bash
npx vite --port 5199            # 먼저 띄운다
node scripts/_probe_b1mat.mjs   # 빔 창·밴드·좌표 실측 덤프
node scripts/_shot_mattoken.mjs # 토큰 단독 프리뷰 (룩 검수용)
node scripts/_shot_b1mat.mjs    # 시뮬 BK_B1 실제 화면
node scripts/export_mat_png.mjs # 합성용 PNG 재생성
npx vite build                  # 통과해야 한다
```

`node scripts/check_content_fit.mjs` 는 **실패 6건**이 기존 상태다(이 작업 이전과 동일).

---

# 이어붙임 — 2026-08-07 (디자인 재작업)

첫 판이 "AI 티 나고 촌스럽다"는 지적을 받고 세 번 갈아엎었다. 무엇을 왜 버렸는지가
다음 사람에게 제일 필요한 정보라 순서대로 적는다.

## 무엇이 남았나

**판(마름모꼴)이 없다.** 프로젝터는 매트가 아니다 — 사각형을 그려 "여기 있다"를
증명할 필요가 없다. 판에 딸려 있던 눈금자·타이틀·코너 브래킷·하단 워드마크도 같이
나갔다. 남은 건 **노드 4개 + 허브 1개 + 방사 레일**뿐이다.

| | 지금 |
|---|---|
| 노드 | 잽잽훅 노드와 **같은 레시피** — 채움 `lut(0.36)` / 활성 `lut(0.5)` + `volRing` + `GB×1.4`. 윤곽선 안 긋는다. 활성 1.34배(콤보 노드 `acc` 배율과 같은 수) |
| 허브 | 링 하나가 곧 진행. 미진행 트랙(alpha .3) 위에 진행 획을 겹친다 — 게이지 원을 따로 두지 않는다 |
| 레일 | 각 노드에서 허브 림 앞까지 방사형 점선(0길이 대시·라운드캡·alpha .22·`lut(0.6)`). 순서대로 잇던 폴리라인은 허브를 가로질러 폐기 |
| 로고 | 허브 정중앙 (`public/newton-symbol.svg`) |
| 색 | 크롬은 `lut(0.86)`. **하드코딩 크림(rgba 255,246,234) 폐기** |
| 두께 | 전부 `LNW = 4 × arrow.w × s` 배수. 랩 LINE 슬라이더가 이 판에도 걸린다 |

## 모션 — 파라미터로 구동한다

```js
P.in      0→1  등장. 허브 먼저(eOutQuint 0.45), 노드 0.09s 스태거. 되감으면 역순으로 닫힘
P.hit     마지막 접촉 이후 초
P.per     박자 주기(기본 0.4) — 수축 링이 이 주기로 좁혀 든다
P.travel  { from, to, k }  타깃 이동. 코멧이 노드 → 허브 → 노드 두 구간을 달린다
targets[].live  코치 지휘 대상. 그 노드만 채워지고 나머지는 후퇴
```

접촉 모션은 **어프로치 링 정본의 구조**다: 수축 링(1.9R → R, 지수 1.6 가속) +
실키 트레일 2겹 + 잠금 핑(팽창하며 소멸). 크기만 튀던 것을 교체했다.
상시 마이크로모션은 허브 호흡 ±0.8% 하나뿐 — **대기 노드는 움직이지 않는다.**
대기가 움직이면 위계가 무너진다.

## 검증

```bash
npx vite --port 5199
# footlab.html → '드리블 매트' 카드. 프레임 아래 버튼 셋: 등장 / 공 닿음 / 타겟 이동
node scripts/_shot_footlab_mat.mjs   # 토큰 프레임 캡처
node scripts/_shot_b1mat.mjs         # 시뮬 BK_B1 실화면
```

## 남은 일 — 이 순서로

1. **모션이 랩에서만 돈다.** `session.js` BK_B1 핸들러에서 실제 바운스에 `P.hit`,
   단계 전환에 `P.in`, 코치 지휘에 `P.travel` 을 물려야 한다. 지금 시뮬은 정적이다.
2. **허브 색이 리터럴이다.** `lut(0.30~0.34)` 를 `deriveSessionPalette` 파생으로
   묶어야 룩 팔레트를 바꿀 때 같이 따라온다. 지금은 노드(주황)와 허브(빨강)가 한
   램프 위인데도 계열이 갈려 보인다.
3. **죽은 파라미터 제거.** 판을 없애면서 `ruler` · `title` · `chev` · `round` 가
   안 쓰인다. `PRIM_DEFAULTS` · `fxlab` 슬라이더에서도 같이 뺄 것.
4. **글로우 반경이 픽셀 직접 지정이다.** `shadowBlur` 를 `look.halo` 파생으로 —
   지금 랩 헤일로 슬라이더를 일부만 탄다.

## 커밋 순서 (읽으면 왜 이렇게 됐는지 다 나온다)

```
368296a  원 하나 대신 훈련 판 토큰
91dc0e6  네온을 걷어내고 유리 알약 재질로
8e64b1d  표적을 판정 토큰으로, 16회로, 시선을 판 위로
caf4e13  눈금자·브래킷 걷어내고 로고는 에셋으로
0a5d78d  원형 메뉴 위계로: 대기는 헤어라인, 선택만 채운다
e3689df  색을 LUT 에서, 두께를 LINE 정본에서, 레일은 제거
572b3a7  판을 없애고 노드만, 로고는 허브 정중앙, 등장·접촉 모션
eae1f86  노드를 잽잽훅 레시피 그대로, 랩에 모션 버튼
c1016e6  이음새를 잽잽훅 문법으로: 점 레일 + 이동 코멧
b800cac  허브는 한 줄, 그 선이 반응한다
04682d7  레일을 방사형으로: 모든 길은 허브를 거친다
cbfaaea  접촉 모션을 어프로치 링 정본으로
```
