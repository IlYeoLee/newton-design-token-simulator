# 인수인계 — 드리블 매트 인터랙션 (BK_B1) · 2026-08-07

앞 문서 `HANDOFF-0806-DRIBBLE-MAT.md` 의 후속. 그 문서의 **남은 일 1·2** 를 처리했고,
그 과정에서 설계가 두 번 뒤집혔다. 무엇을 왜 뒤집었는지가 다음 사람에게 제일 중요하다.

---

## 1. 이번에 확정된 규칙 — 이건 유저가 직접 정한 것이다

### 반응은 **닿은 자리**에서 일어난다
> "3번 드리블 자리에서 고정시켜 내가 잘하는구나를 알고 싶지, 갑자기 중앙에 잘했다는
> 시그널이 보이면 좀 그래."

수축 링·트레일·잠금 핑이 전부 허브(`P.center`)에만 걸려 있었다 — 어느 표적에 튀기든
가운데가 반짝였다. 표적을 5개 둔 의미가 절반만 살아 있었다.
→ `contactFx(cx, cy, R, hit)` 로 뽑아 **노드 단위**로 호출. `targets[i].hit` 이 그 신호다.

### 가운데도 **표적 5번**이다
> "가이드 보니까 가운데에서도 공 할 수 있더구만."

내가 위 수정을 하면서 허브를 반응 대상에서 통째로 뺐는데, 그러면 가운데에 튀길 때 아무
일도 안 일어난다. **과잉 수정이었다.** `center.hit` 을 노드 넷과 같은 자격으로 되돌렸다.
카드 제목부터 "표적 5" 다 — 넷이 아니다.

### 진행 게이지는 허브에 두지 않는다
> "도대체 애매한 가운데 링이 왜 반만 차져 있냐고."

그게 `P.prog` 였다. 가운데가 표적이 된 이상 그 링은 '지금 겨눌 자리'를 말해야 하는데,
거기에 '16회 중 8회'를 겹쳐 놓으니 **한 선이 공간과 시간을 동시에 말해** 어느 쪽도 안 읽혔다.
회차는 헤더 알약이 이미 `/16` 으로 말한다(같은 정보 두 벌 금지).
→ 파라미터는 남기고 호출부(`session.js`·`footlab.html`)가 안 넘긴다.

### 반짝임은 색이 아니라 **구조**다
잽잽훅 노드에는 있는 **바깥 림 스트로크**가 매트 노드에만 없었다("윤곽선을 긋지 않는다"고
일부러 뺀 것). 채움이 `0.88R` 이라 R 까지의 띠가 빈 채로 남아 납작한 원반이 됐다.
채움(0.88R) ↔ 림(R) **사이의 간격이 곧 겹으로 읽힌다.** 수치는 콤보 노드 정본 그대로.

---

## 2. 낮에 안 보이던 것 — 밝기가 아니라 **램프 위치** 문제였다

실측(바닥 `#8B9080` L=141):

| LUT | 색 | 휘도 | Δ바닥 |
|---|---|---|---|
| 0.20 | rgb(250,48,48) | 108 | −32 |
| 0.36 | rgb(251,67,51) | 120 | −20 |
| **0.50** | rgb(253,99,57) | **140** | **0** |
| 0.62 | rgb(255,129,77) | 161 | +20 |
| 0.86 | rgb(254,195,137) | 206 | +65 |
| 0.95 | rgb(229,233,213) | 230 | +89 |

**이 팔레트는 램프 한가운데가 바닥과 휘도가 같다.** 그리고 활성 노드가 바로 그 `lut(0.5)`
였다. 색조는 주황이라 화면에선 달라 보이지만 휘도가 같으면 밝은 데서·멀리서 사라진다.
대비는 양 끝에만 있다.

→ `livePrimEnv` 가 `day` 를 프림 그리기 함수까지 넘기고, 주간엔 활성 `lut(0.26)`(Δ−32) ·
대기 `lut(0.62)`(Δ+20) 로 **양 끝으로 벌린다**. 밝은 면에서 '강함'은 밝음이 아니라 잉크다.

### ⚠ 검증 안 된 부분 — 반드시 눈으로 확인할 것
화소 단위 개선은 **확인하지 못했다**. 전체 평균 대비로 재면 day0 10.3 / day1 10.4 로 차이가
없다 — 평균이 넓고 옅은 글로우 39,382 화소에 지배당해 정작 눈이 읽는 진한 획의 변화가 묻힌다.
알파 0.5 이상만 골라 재려던 계측은 페이지가 계속 리로드돼 못 돌렸다.
**근거는 위 LUT 휘도표(직접 측정)뿐이다.**

또한 **footlab 은 `day` 를 안 넘긴다**(`_primEnv()` 에 없다). 랩 캔버스가 검은색이라 그게
맞지만, 잉크 합성 판단은 **랩이 아니라 시뮬(`?scene=BK_B1`)에서** 해야 한다.

---

## 3. 남은 일 — 이 순서로

### 1) 모션 키감 (유저: "쫀득하진 않네, 잽잽훅은 그랬는데")
가장 큰 미해결 항목이다. `markmotion.js` 에 감쇠 반동(`over`·`settle`·`settleHz`)을 넣어
발자국 쪽은 손봤지만 **매트 노드에는 그 어휘가 안 걸려 있다.** 잽잽훅과 무엇이 다른지
`drawComboNode` 쪽 접촉 모션과 `contactFx` 를 나란히 놓고 비교해야 한다.
후보: 수축 지수(1.6)·트레일 겹수(2)·잠금 핑 수명(0.42s)이 잽잽훅과 다른지.

### 2) 노드별 접촉을 **실제 공 위치**로 판정
지금은 "코치가 지휘 중인 노드 = 접촉한 노드"로 가정한다(`session.js` BK_B1).
실제로는 사용자가 엉뚱한 데 튀길 수 있다. 공 착지 좌표를 노드에 매칭해야 하고,
그때 비로소 "틀린 자리" 피드백이 가능해진다.

### 3) 박자 실측 — **레퍼런스 클립에서 뽑아야 한다**
유저가 준 `~/Desktop/ 이 박.mov` 는 **레퍼런스 영상 플레이어를 찍은 화면 녹화**다
(0:09/0:30 "Now, warm up with low dribbles", 실제 사람이 로우 드리블).
화면 녹화라 프레임 차분에 박자가 안 잡힌다(5.5s 중 피크 1개, 4.43s).
→ 원본 클립 파일을 받아서 거기서 바운스 온셋을 뽑아야 한다. 그 값이 `P.per` 가 된다.

### 4) 5개 중 **어디를 반복해서 때릴지** — 아직 미결
유저 질문 그대로다. 지금 랩 재생은 네 박마다 1→2→3→4→가운데 순회인데, 이건 임시값이다.
실제 로우 드리블 훈련이 어느 자리를 반복하는지는 위 레퍼런스 클립을 봐야 정할 수 있다.

---

## 4. 검증 명령

```bash
npx vite --port 5199
# footlab.html → '드리블 매트' 카드 → [등장][공 닿음][타겟 이동][▶ 재생] + 박자 슬라이더
# scenes.html  → 시점 '평면 (대지 1:1)' → BK_B1   ← 조판·미감 판단은 여기서
node scripts/_shot_b1mat.mjs     # 시뮬 실화면
node scripts/shot_plan.mjs BK_B1 # 평면 추출
```

`scripts/_shot_b1mat.mjs` 의 저장 경로가 Windows 절대경로로 박혀 있던 것을 이 맥 경로로
바꿨다 — 다른 기계에서 돌리면 다시 안 맞을 수 있다(`SHOT_DIR` 환경변수로 빼는 게 맞다).

## 5. 관련 커밋

```
ebc620e  매트 노드에 바깥 림 — 반짝임이 안 나던 이유
ef87f4c  접촉 반응을 닿은 노드로
f5a898a  드리블 매트 모션을 시뮬에 물린다 (in/hit/per/live)
         가운데도 표적 5번 · 허브에서 진행 게이지 제거
         매트 카드에 재생 루프 + 박자 슬라이더
```
