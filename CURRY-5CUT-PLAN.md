# 커리 팩 최소안 — 3컷 (훈련 2 + 실전 1) · 2026-07-28

시간이 없으므로 **새로 만들 것을 하나로 줄인** 최소 구성이다.
컷 1은 이미 구현돼 있고, 컷 3은 컷 2를 재사용한다. 실제 신규 작업은 **스텝백 하나뿐**이다.

| | 무브 | 배우는 것 | 신규 작업량 |
|---|---|---|---|
| 훈련 1 · BK_B1 | `Low Dribble` | 공을 낮게 지배 | **없음 (구현 완료)** |
| 훈련 2 · BK_B2 | `Curry Step-back` | 뒤로 빠져 공간 만들기 | 존 1개 + 판정 1줄 |
| 실전 · BK_C1 | `Step-back Three` | 빠지며 곧바로 3점 | 훈련 2 + 릴리즈 판정 |

빠진 것: 팝 스톱(스텝백 안에 이미 포함된 뉘앙스), 크로스오버(양손이라 차폐 6%),
다리 사이(시그니처 기여 낮음). 커리의 대표 무브는 **스텝백 3점** 하나라 이걸로 충분하다.

---

## 컷 1 · 훈련 — Low Dribble (BK_B1) `그대로 둠`

- 와이드 스탠스, 오른손으로 무릎 아래 10회. 셋업 막(발 모음 → 벌어짐 → Success 배지)까지 완성됨
- 클립 `bp_dribble`(제자리 1.63s 루프), 공 물리·왼팔 축 내림 반영 완료
- 차폐 **0%**

## 컷 2 · 훈련 — Curry Step-back (BK_B2) `유일한 신규 작업`

- **동작**: 앞에 밀다가 뒤로 빠지기. 슛은 없음 — 발만
- **클립**: `rk_stepback` (12.77s, 후진 0.84m = 실제 스텝백 거리). 데모 봇은 `lockInPlace` 제자리 고정
- **지면 UI**: 기존 B2 존 2개를 **앞 스탠스 존 / 뒤 스텝백 존**으로 재배치(좌우 x → 앞뒤 z).
  `floorRing` 그대로 쓰고 좌표만 바꾸면 됨
- **판정 (한 줄)**: `뒷발.z − 골반.z > 0.45` 가 참이 되는 순간 카운트. 5회
- **차폐**: **0%**
- **선행**: `rk_stepback` 12.77s 중 스텝백 1회 구간 찾기(포즈 거리 전수 탐색 스크립트 재사용, 30분)

## 컷 3 · 실전 — Step-back Three (BK_C1) `컷 2 재사용`

- **동작**: 컷 2에 릴리즈를 붙여 풀템포로. 3회 성공 시 종료
- **클립**: `rk_stepback` 스텝백 구간 → `mf_jump_shot`(4.97s, 제자리, 점프 Y 0.74) 교차 페이드
- **지면 UI**: 컷 2와 동일 + **릴리즈 마크**(스텝백 존 안쪽). 발이 존에 닿는 순간 마크 팡
- **판정**: 컷 2 판정 성립 후 **0.4초 안에** 골반 Y 상승 전환(점프). 늦으면 "리듬 놓침"
- **차폐**: **0%**
- **정리**: 기존 BK_B3 / BK_C2 / BK_C3 / BK_C4 스테이지는 흐름에서 제외

---

## 설계를 지배하는 두 제약 (반드시 지킬 것)

**A. 몸에 달린 빔은 상대 정보만 가르친다.** 투사면이 유저와 함께 움직여서 "1m 이동" 같은
절대 위치 가이드는 원리적으로 불가능하다. 실측으로 확인 — 루트 이동 클립(`cmu_dribble_side`,
Z 3.86m)을 넣자 봇이 화면 밖으로 나갔다. 그래서 판정은 전부 **발↔골반 상대 거리**다.

**B. 스텝·슛 계열은 차폐 0%.** 공이 빔을 가리는 건 크로스오버 계열뿐(6%, 45ms 암전 7회/11초).
3컷 전부 발이 판정 대상이라 이 문제를 아예 회피한다. 상세는 `BEAM-OCCLUSION.md`.

## 보유 모션 확인 (런타임 실측, 추정 아님)

쓸 것만: `bp_dribble` 1.63s 제자리 · **`rk_stepback` 12.77s 후진 0.84m** ·
`mf_jump_shot` 4.97s 제자리(점프 Y 0.74). **새 모캡 불필요.**

폐기: `cmu_dribble_side`(Z 3.86m) · `cmu_dribble_fwd`(5.69m) · `bl_crossover`(5.57m)
— 전부 루트 이동 과다로 제약 A 위반.

## 작업 순서

1. `rk_stepback` 스텝백 1회 구간 탐색 → 클립 창 확정
2. BK_B2 존 좌표를 앞뒤(z)로 재배치 + 판정 한 줄 교체
3. BK_C1 = BK_B2 복제 + 릴리즈 판정·마크 추가
4. 관찰 프리뷰 정규식 3곳에 B2·C1 유지 확인
   (`src/main.js` COACH_CFG/COACH_IDS/_watchWin · `floor-scene.html` prev-row · `session.js` 조기리턴)
5. 지면 헤더 문구: `Low Dribble` / `Curry Step-back` / `Step-back Three`
6. `npx vite build` → 4173 확인

## 무브명 근거

커리의 스텝백 3점은 고유명으로 통용된다.
- https://fansided.com/2018/01/24/modern-moves-stephen-currys-step-back-3-pointer/
- https://www.youtube.com/watch?v=YADOYr1WAZM (Pop Stop Step Back)
