# 씬 캡슐 시스템 — 연습·실전 바닥 UI 신규 정본 (유저 지시 2026-08-05, 한 단어도 빠짐없이)

피그마: 파일 `a2Zo9mBTQojjGKaSerQzUa`
- 시작화면 342:3057 · 잠깐 나타남 342:3412 · **프리뷰 343:3496** · **일반 가이드(영상 따라하기) 343:6403** · **바닥 가이드 348:7019** · **농구 미니 프리뷰 343:6447**

## 0. 공통 컴포넌트
- **세션 시간 호(#69)** = `arc-gauge`(넓은 호 + 광점): "현재 진행 중인 세션 시간" 표시. **모든 시스템 공통.**
  기존 floorgl `arcGauge()` 정본 재사용 — 프리뷰(1048×262, 캡슐 상단 y≈197)·바닥 가이드(동일)·전 변형에 배치.
- **필 배지**(#7f7f7f, px40 py10, r999, 64px, 흰 80%): 내용이 변형별로 다름
  - 프리뷰: `Preview` (Regular)
  - 일반/바닥 가이드: **볼드 숫자 = 타이머/스톱워치**(#67, 예 "10") — 각 운동 단계에 맞는 값, 숫자 **counting 애니메이션(rollNum)**
  - 농구 미니: `3/4` — **진행 단계 표시**(#68)
- **캡슐**: READY 캡슐 렌더러 재사용(글로우 4겹 + 유리 림). 변형별 지오메트리:
  - 프리뷰: (162,63) 1276×1955 — READY(291,285 1018×1591)에서 **아주 부드럽게 커지는 모션**
  - 일반/바닥/미니: (291,63) 1018×713 — 상단 컴팩트 캡슐(글로우는 하단 축소판)

## 1. 잠깐나타남/시작화면 → 프리뷰 전환 (343:3496)
1. 캡슐이 **아주 부드럽게 커지면서**(1018×1591@285 → 1276×1955@-15) 모션
2. 타이틀: 팩 타이틀("Sean's Final 1km Pace") → **가르쳐야 하는 타이틀**(운동명, 예 "Neck And Shoulder" 100 Bold 2줄)로 자연스럽게 크로스페이드
3. READY 인물(실루엣) 사라짐 → **프리뷰 인물**(코치 데모판)이 **잘리지 않는 크기·위치**로 재이식 (피그마: (214,766) 1173×923 상당)
4. 상단: Preview 배지 + 세션 시간 호
5. 우측(#66): **백그라운드 블러** 필(backdrop-blur 17, 흰 40%, p30, r1498) 안에 셰브론 3개(75.3px, -30 겹침) — **연했다가 차차착 진해지며 순차 표시** = "다음에 뭔가 있다" 암시
   (캔버스 블러: 필 영역 스냅샷 blur 근사 or 반투명 필로 대체 — 실측 후 결정)

## 2. 실전 3분화
### 2a. 일반 가이드 — 영상 보면서 따라하기 (343:6403)
- 상단 컴팩트 캡슐(291,63 1018×713): 배지 = **볼드 타이머 숫자**(counting), 타이틀 100 Bold 2줄(캡슐 내 y≈493), 캡슐 하단 엠버 글로우
- 인물(코치 데모판) 캡슐 **아래** 크게 (116,853) 1308×1030 상당
### 2b. 바닥 가이드 (348:7019)
- 2a와 동일 + 세션 시간 호(캡슐 상단 y197 겹침)
- 아래에 **넉넉하게 발바닥·화살표 등 토큰 가이드를 넓게**: 왼_액티브(180,1014) 550 · 오_액티브(829,1403) 550 — 기존 스테이지 G그룹 토큰이 담당(캔버스 아님)
### 2c. 농구 미니 프리뷰 (343:6447) — 복잡한 스텝 학습용
- 상단 캡슐(713, top -78 클립): 배지 `3/4`(진행 단계), **타이틀 축소**(80 Bold 1줄, y≈469)
- **작은 영상 미리보기**: 인물 미니(545,208) 569×448 — 캡슐 안, 스텝 프리뷰 반복
- 아래 토큰 가이드(왼_액티브 등)는 그대로

## 3. 구현 슬롯
- floorgl: `_capsule(x,y,w,h,e)` 추출, `_pillBadge(cx,y,text,bold)`, 프리뷰/가이드 페인트는 SCENES 변형 플래그(variant: 'preview'|'video'|'floor'|'mini')로 `_paint_scene` 계열에 통합
- 전환 모션: session READY→A1 진입 시 t 기반 보간(캡슐 지오메트리 lerp + 타이틀 크로스페이드) — 프레임 src 는 유지한 채 floorgl 이 그린다
- 인물: COACH_CFG 변형별 w/h/fwd 재조정(프리뷰=크게, 일반=캡슐 아래, 미니=캡슐 안 작게)
- 배지 숫자: rollNum(도트 아님 — Supreme Bold, 규약 확인 필요) counting
- 미니 영상: 코치 클립 텍스처를 작은 패널로(기존 demoPanel 축소 배치)

## 4. 상태
- [x] 피그마 4프레임 정밀 스펙 확보(위 수치)
- [ ] 공통 프리미티브(_capsule/_pillBadge) 추출
- [ ] 프리뷰 화면 + READY→프리뷰 성장 전환
- [ ] 일반 가이드(타이머 배지 counting)
- [ ] 바닥 가이드(호 + 토큰 존)
- [ ] 농구 미니(3/4 배지·미니 영상·타이틀 축소)
- [ ] 셰브론 넥스트 힌트(#66) 순차 페이드
- [ ] 세션 시간 호 전 시스템 공통 배치
