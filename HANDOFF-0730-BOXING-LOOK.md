# 인수인계 — 2026-07-30 · 복싱 룩 정비

다른 기기에서 이 파일만 읽고 이어갈 수 있게 쓴 문서.
일반 규칙·이전 맥락은 `HANDOFF.md`(07-28), 투사 규격은 `PROJECTION-SPEC.md`,
과거 버그 전말은 `KNOWN-ISSUES.md`.

---

## 0. 새 기기에서 시작하기

```bash
git clone https://github.com/IlYeoLee/newton-design-token-simulator
cd newton-design-token-simulator
npm install
npx vite --port 5199 --strictPort     # 유저는 항상 5199 에서 본다
```

- Node 20+ / Chrome. `puppeteer` 는 devDependency 로 들어 있다(계측 스크립트용).
- 폰트(Supreme·OffBit)·인물 에셋은 리포에 포함. 별도 설치 없음.
- **에셋 주의**: `public/ready-view/assets/` 안의 PNG 는 알파 컷아웃이지만 투명 영역의 RGB 가
  검정이 아니다(`boxer_learn.png` = 그린스크린 잔상, `bk/*.png` = 흰색). 가산 블렌딩으로
  그릴 때 반투명 가장자리에 그 색이 실린다. 새 인물 에셋을 넣을 땐 이걸 먼저 확인할 것.

Claude Code 세션 이어받기:

```bash
claude --resume     # 이 폴더의 지난 대화 목록에서 선택
claude -c           # 가장 최근 대화 바로 이어가기
```

대화 기록은 `~/.claude/projects/<폴더슬러그>/*.jsonl`. **기기가 바뀌면 이 기록은 안 따라온다** —
그래서 이 문서가 필요하다.

---

## 1. 이번 세션이 한 일 (한 줄 요약)

**복싱 투사 UI 의 "색이 죽고 레이어가 따로 노는" 문제를 근본에서 잡고, 학습 3단계(B2 슬립 ·
B3 잽 스윕 · C2 실전 · C3 콤보)를 하나의 룩 언어(수축 링)로 다시 세웠다.**

커밋 범위: `1ee30a1` … `381c15a` (전부 `main` 에 푸시됨).

---

## 2. 반드시 알아야 할 함정 셋

이 세 개를 모르면 같은 삽질을 반복한다. 오늘 전부 실제로 밟았다.

### ① 캔버스 `destination-out` 지우개는 알파 1 이어야 한다

`insetGlow()`(floorgl.js)가 글로우색 `rgba(255,255,255,0.6)` 을 fillStyle 에 둔 채 안쪽을
지우고 있었다. `destination-out` 은 dst 알파를 `(1 − src알파)` 배로 **남기므로** 0.6 지우개는
40% 를 못 지운다 → 림에만 있어야 할 글로우가 카드 **전체**에 흰 24% 로 깔렸다.
`#FA3030`(250,48,48)이 (251,98,98)로 바래 보인 게 이것 때문.

**유저는 "칙칙하다 / 연하다 / 색이 죽었다"를 여러 번 신고했고 나는 그때마다 엉뚱한 데를
지목했다** — 밝은 실내 투사면, 벽 평면 5% 알파, 대지 투명 배경, 블룸, 주간 노출. 전부 무고.
전말: `KNOWN-ISSUES.md` 최상단.

교훈: **부분 지우개의 결과는 '조금 흐려짐'이라 버그로 안 보이고 디자인 실수처럼 보인다.**
색이 이상하면 합성을 의심하기 전에 **캔버스 원본 픽셀부터 재라**(§3).

### ② 백그라운드 탭은 렌더 루프가 멈춘다

원격/헤드리스 탭이 뒤로 가면 `visibilityState=hidden` → rAF 정지 → `rig.update()` 가 안 돌고
`rig._fp`(투사 풋프린트)가 `null` → UI 게이트가 닫힌다. 이 상태에서 `wallGL.mesh.visible` 을
읽으면 `false` 가 나와 **"UI 가 사라졌다"고 오진하게 된다.** 오늘 실제로 그랬다.

판별: `document.visibilityState` 와 `session.t` 가 시간이 지나도 안 변하는지 확인.

### ③ 같은 리포에 편집 세션 여러 개 = 커밋이 섞인다

다른 세션이 `git add -A` 로 커밋하면서 **내가 편집 중이던 파일까지 쓸어갔다**(2회:
`b97aa84`, `95853d6`). 코드는 들어갔지만 커밋 메시지와 내용이 어긋나 추적이 어려워진다.
편집 세션은 리포당 하나. 병렬이 필요하면 `git worktree add ../newton-b`.

---

## 3. 계측 도구 (추측 금지)

### 투사 UI 는 2D 캔버스라 **정확히 잴 수 있다**

WebGL 백버퍼는 `preserveDrawingBuffer:false` 라 못 읽지만, 벽·바닥 UI 는 캔버스 2D 로 그린 뒤
텍스처로 올린다. 그 캔버스만 따로 그리게 하면 3D·카메라·포스트를 거치지 않은 원본이 나온다.

```bash
node scripts/shot_ui.mjs BX_C2 scene out.png 4        # 벽 대지 → PNG
node scripts/shot_ui.mjs --floor T1 transition f.png 3 # 바닥 대지
node scripts/shot_prim.mjs stanceBox sheet.png         # 프리미티브 prog 0~1 시트
```

브라우저 콘솔에서 직접 재는 형태(원격 조종 시):

```js
const M = await import('/src/wallgl.js?v=' + Date.now());
const w = new M.WallGL();
w.stage = 'BX_T1'; w.kind = 'transition'; w.t = 6; w.params = {};
w._paint();
const K = w.canvas.width / 2600;              // 대지 → 캔버스 배율
w.canvas.getContext('2d').getImageData(x * K, y * K, 1, 1).data;
```

**판정 기준**: 중앙과 가장자리 안쪽 값이 **같으면 균일 베일**(= 드로잉 문제),
다르면 합성/포스트 문제. 이 한 줄이 ①을 잡았다.

### WebGL·씬 상태는 앱 안에서만

`window.__dbg`(DEV 전용)에 `session · rig · xbot · camera · scene · wallGL · floorGL · tokens`
가 노출돼 있다. 픽셀이 필요하면 앱 자신의 rAF 안에서 읽어야 한다 — `?blackprobe=1` 이 그 예
(`main.js blackProbe`).

---

## 4. 좌표·규약 (자주 헷갈림)

| 이름 | 값 | 뜻 |
|---|---|---|
| 대지 | 2600 × 1600 px | 벽 UI 저작 좌표. 실물 벽 2.6 × 1.63 m 와 1:1 |
| `K` | 0.75 (`?uiscale=`) | 대지 → 캔버스 해상도 배율 |
| `TS` | 0.92 (`?type=1` 로 해제) | 투사 UI 공통 타이포 스케일. `F()` 한 곳에서만 곱한다 |
| `WALL_Z` | −1.8 m | 벽면 z. 봇은 −Z 를 본다 = **+z 가 사용자 쪽** |
| `WZ` | `WALL_Z + 0.03` | 벽 판정 토큰 평면 |

**복싱 벽은 한 겹이다.** 벽에 투사되는 것(UI 평면 · 인물 · 판정 토큰 · HUD 텍스트)은 전부
`renderOrder = 20` 이고, 앞뒤는 **z 가 정한다**(three 는 같은 renderOrder 의 투명체를 뒤→앞 정렬).

```
z −1.800  벽면
z −1.780  인물 bxPerson (WALL_Z + 0.02)
z −1.770  판정 토큰 (WZ)
z −1.768  HUD 텍스트 (WZ + 0.002)
z −1.750  벽 UI 평면 (WALL_Z + 0.05, main.js 에서 매 프레임 설정)
```

몇 mm 씩 띄운 이유는 셋: 동일 z 는 정렬이 불안정해 깜빡인다 / 같은 밴드에선 이 mm 가 곧 앞뒤다 /
x봇 가림은 깊이 테스트가 한다. 3cm 는 4m 거리에서 시차 0.5% 미만이라 안 보인다.
**이 값을 키우면(10cm+) 비스듬히 볼 때 UI 가 벽에서 뜬 것처럼 어긋난다.**

벽 텍스트 슬롯(`wSlotFS/FL/FM`, `wCount`)은 매 프레임 새 메시라 빌드 때 찍은 renderOrder 가
안 물린다 → `Session._slot()` 이 붙일 때 다시 찍는다(`_wallBand`).

---

## 5. 룩 시스템 — 토큰은 하나씩만 있다

유저 원칙: **"우리 수축 링 디자인은 하나다."** 같은 뜻의 토큰을 두 벌 만들면 화면에서
바로 들킨다(실제로 들켰다).

| 토큰 | 정본 | 쓰는 곳 |
|---|---|---|
| 수축 링(접근·타이밍) | `fx-core.js drawApproachRing` — 타겟 존·수축·잠금 핑이 **한 토큰 안에** 있다. `prog` 하나만 준다 | A3 · B3 · C2 · C3 |
| 스탠스/가드 박스 | `fx-core.js drawStanceBox` — `P.prog` 로 **테두리가 도트를 유지한 채** 상단 중앙부터 시계방향으로 찬다 | B1 |
| 궤적 | `drawTrajectory` (`pts` = 정규 [-1,1] 제어점) | A3 잽 · B2 슬립 · B3 스윕 |
| 콤보 연결·순서 | `drawPunchLine` — 노드 MARK 링 + LINE 연결 + 순서 숫자 | C3 |
| 방향 | `tokens.js makeFlowArrow` | B2 |
| 파동 링 | `session.js wallRing`(셰이더) | 순수 존 표시용. **수축 표현에 쓰지 말 것** — 이걸로 수축을 흉내 내다 링 디자인이 두 종류가 됐다 |

가드 박스 진행을 실선으로 채우면 안 된다 — **도트 리듬을 유지한 채** 점이 늘어나야 한다.
위상은 바탕 테두리(`-t*22*s`, roundRect 좌상단 시작)와 맞춰야 두 겹으로 안 보인다.

---

## 6. 복싱 스테이지 현재 상태

네 축(음성 `STAGES.boxing[].voice` / 벽 `wallgl.js SCENES` / HUD `FS·FL·FM` / 실제 토큰)을
대조해 어긋난 곳을 이번에 정리했다. **새 스테이지를 손대면 네 축을 같이 고칠 것.**

| 스테이지 | 지금 무엇을 그리는가 |
|---|---|
| A1/A2/A3 | 목·어깨 회전(rotate) · 스텝 · 잽 궤적 + 수축 링 |
| **B1** 가드 유지 | 가드 박스. **테두리 도트가 차오르는 것**이 3초 유지 진행. 박스 안 원형 링은 폐기 |
| **B2** 회피 슬립 | 한쪽에서 위협 링 수축(1.85→1.0) → 비트에서 팽창 소멸 · 머리가 반대편으로 가라앉는 U 궤적 · 안전 존 점등 · 피할 방향 화살표만 점등. 비트 1.1s × 6 |
| **B3** 잽 스윕 | 스윕 궤적이 0.78 지점에 도착 = 타격. 타겟은 수축 링 정본(`bxB3ap`) |
| **C1** | 3초 카운트다운 |
| **C2** 실전 잽 | 벽 **다섯 곳**(얼굴 셋 · 보디 둘)에 수축 링. 비트 1.9s 마다 한 곳만 살고 쉬는 자리는 알파 0.10 으로 남는다. 직전과 안 겹치게 뽑는다. 스테이지 11s |
| **C3** 콤보 | 펀치 라인(잽·잽·훅 노드 3개) + **수축 링 하나가 지금 노려야 할 노드로 옮겨 가** 그 원 안에서 터진다. 0.52 / 0.90 / 1.58s, 사이클 2.4s. **바닥 스텝 마크는 이 구간만 숨긴다** |
| C4/FIN | 쿨다운 · 리포트 |

벽 수치 열: 아바타 밑 **배지**가 주체를 말한다(`Coach` / `You` — 같은 컴포넌트 복제).
단위 줄은 단위만. 숫자는 **오도미터 롤**(자릿수 휠, 1의 자리만 계속 돌고 윗자리는 자리올림에만).

---

## 7. 남은 일

1. **C2 벽 수치가 고정값** — `SCENES.BX_C2` 의 `coach 8 Thrown / you 5 Landed` 는 실제 판정과
   무관하다. 라이브 판정(`judge.js`)에 물릴지 데모 고정으로 둘지 유저 결정 필요.
   물린다면: session 이 `wallStat` 을 쓰고 → main 이 `wallGL.live` 로 전달 → `_paint_scene` 이
   `this.live?.you ?? S.you.num` 을 쓰는 경로가 가장 짧다(설계만 해둠, 미구현).
2. **"Tap X2 For Retry!"** — 길다는 지적이 있었으나 Figma 대지에도 같은 문구라 보류.
3. **움직이는 화면 검증** — 이번에 바꾼 것들은 코드·정지 픽셀로만 확인했다. 실제 재생 리듬
   (슬립 템포 1.1s, C2 2초 간격, 콤보 0.52/0.90/1.58)은 유저가 눈으로 봐야 한다.
4. **러닝·농구는 안 건드렸다** — 3인칭 프레이밍만 전 종목 공통으로 바뀌었다(아래).

---

## 7.5 영상·UI 내보내기 (실사 합성용)

화면 녹화는 쓰지 않는다 — rAF 가 실시간에 묶여 프레임이 빠지고, 모니터 해상도에 갇히고,
녹화 코덱이 한 번 더 압축한다. 대신 **시계를 직접 밀면서 한 프레임씩** 렌더한다(드롭 0 · 4K 자유).
같은 명령 = 같은 프레임(결정론적). 전제: `npx vite --host 127.0.0.1` 이 5199 에 떠 있을 것.

### 어느 쪽을 쓸까

| | `scripts/export_ui.mjs` | `scripts/export_video.mjs` |
|---|---|---|
| 대상 | 투사 UI **2D 캔버스만** | 3D 씬 전체 |
| 판정 토큰(셰이더 메시) | 안 들어옴 | 들어옴 |
| 앨리어싱·검은 띠 | 원천적으로 없음 | `--ss` 로 잡는다 |
| 쓸 때 | 지면/벽 그래픽만 필요 | 봇·빔·토큰이 같이 필요 |

### 자주 쓰는 명령

```bash
node scripts/export_ui.mjs --list                                  # 뽑을 수 있는 화면 목록
node scripts/export_ui.mjs --stage A3 --dur 8 --fps 60 --w 2160    # 지면 UI
node scripts/export_ui.mjs --surface wall --stage BX_T1 --fps 60   # 벽 UI
node scripts/export_video.mjs --sport boxing --flat --w 2600 --fps 60   # 정면 직교(토큰 포함)
node scripts/export_video.mjs --sport running --beam --w 3840      # 투사광만(에펙 Screen 합성)
```

### 함정 (실측으로 밝혀진 것)

- **`--uiscale` 을 렌더 해상도에 맞추지 말 것.** 벽 기준 5200×3200 = 66MB 텍스처를 매 프레임
  올리게 되고 블룸 타깃까지 겹쳐 **74프레임에서 GPU 컨텍스트를 잃는다**. 기본값(출력의 1.5배)이면
  선예도는 그대로고 메모리는 절반이다.
- **대지 캔버스는 기본 `K=0.75`** 로 그려진다(실시간 예산). 그대로 뽑으면 2600 출력에 1950 원본을
  늘리는 꼴이라 흐리다 — 두 스크립트 모두 출력보다 크게 그린 뒤 줄인다(수퍼샘플링).
- **녹화용 DOM 청소는 매 프레임 다시 해야 한다.** `session.start()` 나 클립 재생이 *나중에* 새 DOM 을
  만든다 — 한 번만 쓸면 클립 미리보기 패널이 우상단에 검은 알약으로 남는다. 인라인 `style` 로 숨기면
  앱이 `el.style.display` 를 다시 쓸 때 통째로 날아가므로 **스타일시트 `!important` 규칙**으로 숨긴다.
- `--flat` 은 화면비를 투사면 대지 비율로 잡는다. 16:9 로 두면 인물이 늘어난다.

---

## 8. 이번 세션 커밋 지도

| 커밋 | 내용 |
|---|---|
| `1ee30a1` `96fa358` | 타이머·리포트 링 = 공통 `ringGauge`(트랙 끝 페이드 + 꼬리 페이드 + 글라스 마커) |
| `0e078c1` | 아크 게이지 눈금·수치 텍스트 제거 + 박스를 잉크에 맞춰 조임 |
| `ba35435` | 카드 인물 블렌드 multiply → **plus-lighter** 원복 · 타이포 공통 0.92 |
| `f8fba66` | **★ insetGlow 지우개 알파** — 카드 빨강이 죽던 진짜 원인 |
| `eebcf25` | 위 전말을 `KNOWN-ISSUES.md` 에 기록 |
| `207d23d` | 코치·유저 카운트 = 오도미터 롤 |
| `160e1bd` | 복싱 4축 문구 검수(B2·B3·C1·C3·A1) |
| `95853d6`(섞임) | 복싱 벽 = 한 겹(renderOrder 20 + z 순서) |
| `b7c4989` `6eeeb25` `cd35ab4` | B3 수축 링 · C2 다섯 곳 무작위 · C3 노드 안 판정 |
| `90c06d8` | 수축 링 정본 통일 · 펀치 라인 복귀 · 가드 박스 테두리 채움 · 좌우 주체 |
| `221ecae` | 가드 박스 채움을 **도트 유지**로 |
| `f561c3f` | C3 구간 바닥 스텝 마크 숨김 |
| `ea8a7be` `381c15a` | 주체 라벨 → 아바타 배지로 통합 · 오도미터 창 정리 |

---

## 9. 작업 규칙 (유저 지시, 계속 유효)

- **UI 배치·크기·색은 브라우저 검증 없이 코드만 넣고 넘긴다.** 단, **버그 원인 규명은 반대** —
  계측 없이 손대지 않는다(§3).
- 하드코딩 금지. 지면·벽 UI 는 룩 시스템 토큰만 쓴다.
- 같은 뜻의 토큰을 새로 만들지 않는다. 있는 정본을 옮겨 쓴다.
- 커밋은 자주, 대화 끝마다 푸시.
- 유저는 5199 에서 본다. 수정 후 "5199 새로고침" 이라고 알릴 것.
