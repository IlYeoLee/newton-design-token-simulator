# 인수인계 — 2026-08-07 · A2 종아리 **뽑기**에서 밟은 함정 전부

`HANDOFF-0807-A2-CALF.md` 의 후속. 그 문서는 **무엇을 뽑는가**(실측 타이밍·스탠스)를 다루고,
이 문서는 **어떻게 뽑는가**에서 오늘 하루에 다섯 번 밟은 것들을 다룬다.

> 새 기계에서 리포만 클론해 A2 를 뽑으려다 5번 실패하고 6번째에 성공했다.
> 실패는 전부 **문서에 없던 것** 때문이었다. 그래서 이 문서가 있다.

---

## 0. 정답 명령 — 이것부터 복사할 것

```bash
npm run dev:export        # 5200. 렌더는 반드시 여기에 붙인다(5199 는 저장 한 번에 죽는다)

node scripts/export_video.mjs --scene A2 --flat --play --beam --grab --pin \
  --t0 0 --dur 16.1 --fps 29.97 --w 1600 --pad 1.25 --ss 1 \
  --url http://127.0.0.1:5200/ --out out/A2

# 파형은 따로. 판에서는 끄고, 계산으로 굽는다(3초).
node scripts/export_video.mjs ... --norip --out out/A2_MARKS
node scripts/bake_footrip.mjs <판PNG> --px 2048 --dur 1.4 --gain 1.5
```

**뽑고 나서 반드시 이 셋을 잰다.** 눈으로 보고 넘기면 여섯 시간을 잃는다(오늘 그랬다).

```bash
node <바탕화면>/qa_frames.mjs <시퀀스>     # 코너색·비검정%·용량
node <바탕화면>/qa_motion.mjs <시퀀스>     # 정지 구간 (얼어붙은 프레임 잡기)
node <바탕화면>/qa_pin.mjs   <시퀀스>      # 좌/우 마크가 실제로 고정됐나
```

---

## 1. 다섯 번의 실패 — 증상 → 진짜 원인

| # | 증상 | 진짜 원인 |
|---|---|---|
| ① | 결과가 **초록 판** | `--beam` 누락. 주석은 "검정배경(기본)"이라 적혀 있었다 |
| ② | 타이틀 알약만 있고 **아무것도 안 움직임** | `export_ui.mjs` 경로로 뽑았다 — 그건 **세션을 안 돌린다** |
| ③ | **17.2초**인데 뒤가 반복 | `--dur 17.2` 는 4렙 시절 값. 되감김을 f432(14.4s)에서 실측 |
| ④ | 상하좌우 **잘림** | 직교 절두체가 대지에 딱 맞는다. `--pad` 로 넓힌다 |
| ⑤ | 발자국 **바들바들 떨림** | `--pin` 누락 → 준 뒤에도 안 먹음(§3) |

### ② 가 제일 오래 걸렸다 — 두 경로를 헷갈리지 말 것

| 경로 | 세션 | A2 에 쓸 수 있나 |
|---|---|---|
| `export_ui.mjs` (`run_floor_ui.mjs`) | **안 돌림** | ✗ **타이틀 알약만 나온다** |
| `export_video.mjs --flat` | `--play` 로 돌림 | ✓ |

`run_floor_ui.mjs` 의 화면 목록에 `{ id: 'A2', title: '준비운동 2/3 — 종아리 늘리기' }` 가
등록돼 있어서 그게 A2 전용 경로처럼 보인다. **아니다.** A2 의 내용물(발자국·홀드 링·3·2·1)은
전부 세션이 만든다. 실측: 240프레임 중 f85 이후 **155장이 바이트까지 동일**했고,
그 정지 화면에는 `CALF STRETCH` 알약 하나뿐이었다. 움직인 2.8초는 알약 등장 애니메이션이다.

---

## 2. 길이는 14.4 도 17.2 도 아니다 — **코드가 말하게 하라**

```js
// src/main.js
session._a2LoopNeed = A2_WATCH + 2 * CYC;   // 관찰 + 2렙
```

`CYC` 는 `stageTime('A2').hold` 에서 파생된다. 홀드가 바뀌면 전체 길이도 바뀐다.

| 홀드 | 1렙(CYC) | 전체 |
|---|---|---|
| 2.15 (실측 클립) | 4.30 | 14.4s |
| **3.0 (유저 확정 08-07)** | **5.15** | **16.1s** |

되감김 지점은 **재서 확인한다**(`f0` 와 각 프레임의 차이가 뚝 떨어지는 곳):

```
f0 ↔ f00426   69.42%
f0 ↔ f00432   28.28%   ← 여기가 루프 시작 (t=14.40s, 홀드 2.15 시절)
```

### 발 교대는 **있다** (08-07 유저 확정)

앞 세션이 화소 추적으로 '교대 없음'이라 결론내고 미러 패리티를 걷어냈다.
유저가 화면을 보고 아니라고 했다 — *"오른발 구부리고 왼발 쭉 3.몇초 피고 다음 다리로 넘어와서"*.

추적이 틀린 게 아니라 **본 것이 달랐다.** 추적은 뒷발이 거의 제자리인 것만 봤다.
옮겨가는 것은 발이 아니라 **역할**이다 — 마크는 제자리고 자막 LEFT/RIGHT · 홀드 링 ·
카운트다운 · Success 가 반대발로 간다. `workLeft` 하나만 뒤집으면 소비자
(`floorgl:3600`, `session:2785`)가 다 따라온다.

**봇 미러(`xbot.group.scale.x`)는 고정 유지.** 회차마다 뒤집으면 사람이 순간이동한다.
그리고 `--beam` 판에는 봇이 안 나오므로 화면에 영향도 없다.

---

## 3. `--pin` 은 플래그만으로는 안 먹는다 (0806 ① 종결)

배선은 멀쩡하다: `FLAGS.__pin` → `Object.assign(window, …)` → `__isolate3d` →
`session.pinMarks`. 그런데도 마크가 움직인다. 실측(좌우를 **나눠서** 재야 한다):

```
왼쪽   x 306px · y 325px
오른쪽 x 206px · y 310px
```

> ⚠ 프레임 전체 무게중심 하나로 재면 **발 교대**까지 '이동'으로 잡힌다.
> 화소 수가 2만~50만으로 요동치면 그 신호다. 반드시 좌/우를 갈라서 잴 것.

A2 마크 z 를 쓰는 곳이 여럿이고(`session.js:3057` 계열) 그중 하나가 핀 뒤에 덮는다.
**원인 코드 특정은 아직 안 됐다.** 다만 추출은 확정돼야 하므로 `__isolate3d` 에서 못박는다 —
이 함수는 `renderer.render` 에 후킹돼 있어 그 프레임의 **마지막 쓰기**다.

```js
if (window.__pin && d.session?.a2press) {
  const P = d.session.a2press, CZ = -1.03;   // FOOT_Z(-0.75) − 0.28 · session.js A2 와 같은 값
  P.fmL.group.position.x = -0.16; P.fmL.group.position.z = CZ;
  P.fmR.group.position.x =  0.16; P.fmR.group.position.z = CZ;
}
```

**위치만 고정한다.** scale·글로우는 그대로 둔다 — 애니메이션은 살아야 한다(유저).

---

## 4. 해상도 — "2K" 라는 말이 이 판에서는 함정이다

지면 대지는 **세로**(1600×2670)다. 폭으로 2K 를 잡으면 화소 수가 4K 를 넘는다.

```
2301 × 3840 = 8.84 M화소     ← 4K UHD(8.29M)보다 크다
2000 × 3338 = 6.68 M화소     ← --w 1600 --pad 1.25
1600 × 2670 = 4.27 M화소     ← 대지 원본
```

`run_floor_ui.mjs` 의 `--w 4602` 주석이 "4K 합성용"이라 적혀 있는데, 그 값의 **높이가 7680**이다.
폭으로 말하면 오해가 생긴다. **화소 수로 판단할 것.**

### PNG 시퀀스를 에펙에 그대로 주지 말 것

2000×3338 장당 9.3MB × 432장을 AE 가 프레임마다 디코드하다 메모리로 튕긴다(유저 실측).
ProRes 단일 파일로 묶어서 준다.

```
PNG 시퀀스        3,646 MB
ProRes 4444(기본)  1,925 MB
ProRes 422 HQ      1,289 MB   ← 알파 없는 검정배경 판엔 이게 맞다
```

```bash
node_modules/ffmpeg-static/ffmpeg.exe -y -framerate 29.97 -i f%05d.png \
  -c:v prores_ks -profile:v 3 -pix_fmt yuv422p10le \
  -color_primaries bt709 -color_trc bt709 -colorspace bt709 out.mov
```

> 용량이 이상하면 **PNG 헤더부터 뜯어라**(비트깊이·컬러타입). 8비트 RGBA 인데 압축률이
> 33~38% 면 정상이 아니라 **거의-검정 노이즈**다 — 화면의 60%가 휘도 16 이하인데
> 순검정이 아니라 1~15 로 흔들려 압축이 안 된다. 바닥컷하면 절반이 준다(실측 12.8→6.8MB).

---

## 5. 파형은 렌더가 아니라 **계산**이다 — 그래서 안 잘린다

파형(`uRip`)은 원형 팡과 달리 **마크 셰이더 안에** 있어 `--layer` 로 못 뺀다.

```bash
node scripts/export_video.mjs ... --norip          # ① 판에서 끄고
node scripts/bake_footrip.mjs <판PNG> --px 2048    # ② 따로 굽는다
```

`bake_footrip.mjs` 는 **뽑아둔 판에서 실루엣을 따서** 거리장을 만든다. 자기 캔버스에 그리므로
투사 절두체와 무관하다 — **잘림이 원리적으로 없다.** `--px` 로 원하는 만큼 여유를 준다.
세기·크기·색을 바꿔도 20분이 아니라 3초다.

에펙에서 판은 Normal, 파형은 **Add**. (둘 다 가산광이라 잉크로 얹으면 바닥을 어둡게 칠한다.)

---

## 6. 룩 — 전체 스냅샷을 적용하면 사흘이 사라진다

`src/mark-versions.js` 의 `v03` 은 **델타가 아니라 08-02 시점의 전 키 스냅샷**이다.
누르면 그 뒤 사흘의 수정(압력 매끈함·상태 크로스페이드·림)이 통째로 되돌아간다.

오늘 실제로 그렇게 됐다: `plantar` 가 0.55 → 0.84 로 돌아가 **고춧가루**(빨강 섬 +
날카로운 경계)가 다시 났다. 압력 정본은 `HANDOFF-0807-STANCE-TOKEN` §4① 의 실측값이다.

```
loadGain 0.45 · loadBase 0.06 · plantar 0.55     ← 섬 소멸, 연속 그라디언트
```

**버전은 델타로 얹는다.** `v0`(09:25 단색 순백 도트)은 `{edgeShade 1.4, edgeShadeW 4, dotMode 0}`
세 키뿐이라 안전하다.

### 각인(무늬)이 사라졌던 이유

농구 발 벌리기 작업에서 '고춧가루'를 잡으려고 `imp 0.98 → 0` 으로 껐다.
**각인은 base 룩이라** 러닝 A2 발자국도 같이 무늬를 잃었다.
`SUCCESS_CANDS.m4`(매끈·각인 유지)가 그 대응이다 — 압력 매끈함은 지키고 각인만 살린다.

---

## 7. 같은 폴더에 두 세션이 붙으면 안 된다

오늘 하루에만:

- 렌더가 상대 저장 순간에 걸려 죽었다(`Runtime.callFunctionOn timed out`, 프레임 0장)
- 상대가 `v03` 을 정본에 적용해 이쪽 압력 수정이 되돌아갔다
- 같은 파일(`mark-look.json`)을 양쪽이 만졌다

5200(`dev:export`)은 **리로드**를 막을 뿐 **반쪽 저장된 코드**는 못 막는다.
긴 렌더를 돌릴 거면 `git worktree` 로 폴더를 갈라라. 같은 커밋을 보되 서로 안 밟는다.

---

## 8. 남은 것

- **`--pin` 의 진짜 범인 미특정.** `__isolate3d` 강제는 우회다. `session.js:3057` 계열
  (`H.fmL.group.position.z = FOOT_Z - FOOT_SWING * H._pL`)이 A2 에서도 도는지 확인할 것.
- **발자국별 섬세한 애니메이팅**(유저 요청, 미착수). `markmotion.js` 에 감쇠 반동
  (`over`·`settle`·`settleHz`) 어휘가 이미 있다 — A2 마크에 물리면 된다.
- **눈높이 미실측.** 러닝 실사 리그(`ae_running_tilt.jsx`)의 `EYE_M 1.55` 는 추정이다.
  기울기에는 영향이 없고 크기만 정한다. 레인 폭(표준 1.22m)이 몇 번째 레인인지 알면 역산된다.
- **배포가 뒤처져 있다.** gh-pages 가 08-07 05:11 시점(소스 `80948c7c`)이라 73커밋 뒤였다.
  유저가 배포본에서 확인하고 있었고, 그래서 "반영이 안 됐다"고 보였다.

---

## 9. 러닝 실사 기울기 — 소실점 실측 (신규)

`HANDOFF-0806` ③ 의 *"실사 레인선의 소실점에서 카메라 피치·화각을 역산해야 한다"* 를 처리했다.

```
소재  Downloads\러닝보정 Topaz Gigapixel 4x 비율.png  (6688×3764)
소실점 (847, -31)  [1672×941 기준]  → 원본 (3388, -124)
지평선 화면 중심보다 501.5px 위 · 가로 편차 11px(0.7%) = 트랙 정면
잔차   최대 10.6px = 폭의 0.6%
```

`f` 와 하향각은 한 쌍이다 — 화각을 정해야 각이 정해진다:

```
40° → 21.20°    50° → 26.43°    60° → 31.61°    70° → 36.73°
```

**60°** 를 기본으로 둔다(리포 1인칭 렌더 `main.js fpMode` 와 같은 값이라 시뮬과 안 어긋난다).
→ **하향각 31.61°**. 에펙 리그는 `ae_running_tilt.jsx`(바탕화면), 측정기는 `measure_track_vp.mjs`.

> 레인선은 **색으로** 잡아야 한다 — 트랙은 초록(G>R), 선은 무채색.
> 밝기로 자르면 펜스·잔디까지 47개를 주워 소실점이 화면 밖으로 나간다(실제로 그랬다).
